# Decisions — cli-0.25.16-vendor-refresh

**Status:** LOCKED 2026-07-07 (design already decided by the adversarial compatibility review — Opus report §2 + Fable conclusion §3, Codex/Fable-vetted; operator authorized executing all corrections + version bump). Deviations from the review are flagged per-decision.

| # | Decision | Rationale | Alternatives rejected |
|---|---|---|---|
| D1 | **Version bump = 4.1.0 (operator-directed 2026-07-08, superseding the originally-planned 3.32.0 minor), executed at DEPLOY by the operator/orchestrator — NOT in the implement handoff** | Precedent: the 0.25.9 re-vendor shipped as MINOR v3.30.0, so the technical change class here (vendored-content refresh + doc corrections, no breaking Flow API change) defaulted to a minor bump. The operator directed a **4.1.0 MAJOR** release on 2026-07-08, which governs the release number (operator directive > semver default for this accumulated branch). The bump rewrites FLOW_RULES.md's live banner via `sync-version-strings.sh` — FLOW_RULES.md is FR-07-protected, so the write needs the write-bootstrap-approval flow, DP.6 magic-phrase confirm, and a DP.1 approval artifact: Deploy-phase work by definition. `.claude-plugin/plugin.json` + `marketplace.json` bump manually alongside (preflight enforces ==VERSION parity) | PATCH (understates vendored contract changes); bumping during implement (violates FR-07 write-gating + role boundary) |
| D2 | **FR-A is ATOMIC: all 36 copies + 4 deletes + 2 adds + manifest re-stamp in ONE commit (T1)** | The review's only identified hazard (F-6) is partial application — manifest/tree/mirror inconsistency is "the one way to CREATE a break out of this review". Mandatory internal order: (1) copy 18 files → `.claude/skills/` (2) same 18 → `.agents/skills/` (3) deletes+adds both mirrors (4) `stamp-cli-provenance.sh` re-stamp. Gate proves self-match (HEALTHY + 0 CLI_SNAPSHOT_STALE + asset_count 130) | Split per-skill commits (windows of inconsistency); manifest-only stamp without copies (certifies stale content — rejected already at v3.30.0) |
| D3 | **Surgical `cp` of the 18 named files + `rm -f` of the 2 named runbooks + `cp` of the 1 named add — NO `rm -rf` of any skill dir** | Destructive-op policy: `rm -rf` on a skill dir could take out non-drifted files or (worse, on a wrong path) Flow-authored content; the drift set is exactly enumerated, so surgical ops are strictly safer and equally sufficient. Byte-identity gate (`diff -rq --strip-trailing-cr` per skill vs CLI source = empty) proves the surgical set was complete | `rm -rf <skill>/ && cp -R` (unbounded blast radius, banned); `cp -R` over dirs without deletes (leaves the 2 retired runbooks behind) |
| D4 | **FR-E strictly AFTER FR-A (T3 after T1), same ticket** | Bumping the 5 strings first would falsely claim 0.25.16 over a 0.25.9 snapshot. Review: "If FR-A is skipped, skip FR-E too." At `docs/compatibility.md:51` KEEP the dated 0.25.9 history line and ADD a new dated 0.25.16 line (dated changelog lines are history — never rewritten) | Editing :51 in place (falsifies history); FR-E in the FR-A commit (mixes vendored-asset + prose concerns; one-task-one-commit) |
| D5 | **FR-C1 phrasing: "marker pair present inside a fenced usage example; the fence-agnostic `CUSTOM_BLOCK_REGEX` captures/restores it" — NOT "extension point"** | Fable conclusion flag #3: the 0.25.16 template block (`AGENTS.md:40-42`) is a fenced *usage example*; behaviorally captured/restored, but calling it a semantic extension point would over-claim CLI intent | Opus's original "ships an empty CUSTOM:SKILL block" phrasing (behaviorally right, intent-misleading) |
| D6 | **CO-2: KEEP test filenames (`test-cli-0259-compat.sh`, `test-cli-flow-recovery.sh`); update in-body labels/comments only** | Rename risk evaluated: `test-cli-0259-compat.sh` is wired by name at `run-tests.sh:383` (+ the label at :428 for recovery); a rename touches run-tests wiring and would recur at EVERY future CLI bump — the encoded model ("0.25.9-era wired Stop set") is version-stable (0.25.16 wires the identical set), so the filename is a historical model name, not a staleness bug. Labels/comments get "unchanged through 0.25.16" annotations instead. `test-cli-flow-recovery.sh:66/:74` `0.25.5` sentinels are intentional older-CLI fixtures — untouched | Rename to `test-cli-02516-compat.sh` + rewire run-tests.sh:383 (churn at every CLI bump, zero behavioral gain, new wiring-typo risk) |
| D7 | **docs/changes note: YES — `docs/changes/2026-07-07-cli-0.25.16-guidance-shift.md` (T5)** | The 0.25.16 guidance shift touches the auth/sign-in domain (server-side magic-link activation replacing client-side `activateAppMagicLink`; `apps[].id` CLI write-back; gate SDK `^v2.3.28-sdk.1`). A dated Flow-side record of when vendored guidance switched models is cheap and load-bearing when debugging apps built against 0.25.9 guidance. NOT added to `docs/changes/index.md` (that ledger is Lightweight-lane-only; this is Full-lane) | No note (loses the only Flow-side dated marker of the auth-model switch); putting it in the spec only (specs are ticket-scoped, not a guidance-shift timeline) |
| D8 | **Problem catalog: NO entry** | Catalog entries record problems Flow encountered AND solved (diagnosis + fix). Here the platform evolved its auth flow upstream; Flow fixed nothing platform-side — a "we-fixed-X" entry would misrecord provenance and dilute catalog retrieval. T5 carries the knowledge in the correct dated-changes home | Catalog entry framed as "platform auth-flow evolution" (wrong artifact class: no Flow-solved problem exists) |
| D9 | **CO-4 lands as documentation in `docs/fusebase-health/ARCHITECTURE.md`, NOT as new keys in `agent-surface-ownership.json`** — deviation from Opus §2 CO-4 file cite, flagged | The gates to document (CLI `copy-template.ts:217` `FLAG_GATED_PATH_PREFIXES`: `managed-integrations/references/personal-auth-flow.md` ← flag `managed-integrations-personal-auth`, `examples/isolated-sql-rls` ← flag `postgres-rls`; plus the `audiences:` frontmatter gate — currently inert, 0 vendored dashboards refs carry it) are provably invisible to the present-file-only engine. Adding documentation-only keys to a data-driven policy JSON consumed by `check-cli-flow-conflicts.sh`/`stamp-cli-provenance.sh` buys zero behavior and adds parser-tolerance risk; markdown docs achieve the clarity aim at zero risk. ARCHITECTURE.md is already edited by CO-3 (same file, one commit) | New `flag_gated_path_prefixes` JSON key with `_comment` (needs consumer-tolerance audit for a cosmetic); leaving undocumented (the review flagged the clarity gap) |
| D10 | **CO-3 scope extended to `ARCHITECTURE.md:22` (agent names) in addition to :21 (skill names)** — verified extension beyond Opus cite, flagged | Line :22 lists `feature-architect.md`/`feature-create-checker.md`; the 0.25.16 CLI ships `app-architect.md`/`app-create-checker.md` (verified 2/2 identical vendored). Identical staleness class, adjacent line, same file/commit | Strict :21-only (leaves a same-class stale claim one line below a freshly fixed one) |
| D11 | **Client-facing vendored UX guidance is re-vendored VERBATIM (byte-copy); `app-ui-design` untouched** | `fusebase-portal-specific-apps` carries client-portal UX guidance and is in the drifted 7 — the CLI owns vendored provider content; Flow injects no UX opinions. Enforced by the AC1 byte-identity gate. `app-ui-design` verified in the SAME-13 set (already byte-identical to 0.25.16) — not touched | Flow-side edits/annotations inside vendored skills (violates the vendored-content ownership boundary + clean-room posture) |
| D12 | **Implement handoff verifies the CLI source tree before any copy (T1 step 0)** | The re-vendor source lives in a session scratchpad; if absent or version-mismatched at implement time, the implementer STOPS (BLOCKED-AT) rather than guessing — checks the tree exists and `apps-cli-main/package.json` says `"version": "0.25.16"` | Copying without provenance check (could silently vendor the wrong CLI version — the exact failure class this ticket fixes) |
| D13 | **FR-C1 / FR-C2a / FR-D + CO-3 / CO-4 applied ON-DISK in gitignored `docs/fusebase-health/` (`.gitignore:55`, "local-only operator notes, never published"), NOT committed — decided at implement time when the gitignore was discovered** | `CLI-CONFLICT-ANALYSIS.md` + `ARCHITECTURE.md` are wholesale-gitignored operator working notes (`git ls-files docs/fusebase-health/` = empty); the corrections make the operator's LOCAL reference accurate — publishing them would flip an explicit maintainer policy for the whole dir. `git add -f` was correctly refused. T2/T4 commit subjects cite these on-disk edits (the edits exist, on disk); the tracked diffs are the publishable subset — disclosed to the operator. The published-surface halves (FR-B, FR-C2b) already landed tracked in `docs/fusebase-cli-edition.md`, so the tracked/on-disk split is principled (published caveat → published doc; local corrections → local analysis docs). | Un-ignore the dir (flips publication policy far beyond ticket scope, against `.gitignore:55`); relocate to a tracked doc (forks content — the published halves already tracked); drop (leaves proven-wrong claims in the operator's local reference) |
