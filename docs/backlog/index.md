# Product Backlog Index

| Slug | Status | One-liner |
|---|---|---|
| carrier-aware-approval-binding | open — replaces consumer proposal E1, DECLINED as proposed | Binding is enforced only when the artifact's author recorded it (`approval_artifact.py:151-161`: *absent/empty recorded value => not bound => ok*), so an unbound artifact authorizes its action in any checkout indefinitely — consumer measured **42/42 unexpired deploy artifacts unbound**, accepted under `strict_approvals: true`. The exposure is real; the proposed global `require_binding` knob is WRONG-LAYER (Codex xHigh premise review, run BEFORE any code). A global "both bindings required" predicate is invalid for the path, bootstrap and reporting carriers, which structurally supply different facts — it would reject the tree-digest-bound artifact `write-bootstrap-approval.sh` mints, breaking the FR-07 protected-path flow. Its fail-safe (unparsable => true) bricks its own recovery: `path_policy` reads the policy before examining artifacts and `approve-local.sh` loads it before writing one. `is_acceptable(verdict, strict)` cannot even express the question — `VALID` does not distinguish bound from unbound. K2 already rejected immediate mandatory binding for invalidating every legacy approval without warning; the flip would change its locked fallback, and K19 already binds new command-gated artifacts. Binding is replay reduction, NOT authorship — K3 records that a same-principal process can hand-write a correctly bound artifact. Replacement is a 7-slice carrier-aware design starting with a K2 revision; resolve together with `compat-approval-surfacing` |
| consumer-post-upgrade-gate-hook | filed, NOT built — a new extension point is a new contract | Consumer proposal E2. `hooks/local/bootstrap-upgrade.sh:750` is `exec bash "$ENGINE_SRC" …` as the file's LAST statement, and `hooks/local/upgrade.sh` ends `UPGRADE_FINISHED=1; trap -; exit 0` (`:815-818`) — so **nothing a consumer installed can run after the hop**, and `upgrade.sh` / `post-fusebase-update.sh` are managed paths the engine refreshes anyway. Gap **CONFIRMED at v4.14.0** (their `:714-750` citation against `4.12.0+1` maps unchanged); the `*.local.*` never-refreshed convention they build on is real too (`upgrade.sh:18,306,789`). Ask: one `hooks/local/post-upgrade-gate.local.sh` run by the SOURCE engine after the apply loop, non-zero ⇒ engine reports FAILED, never under `--dry-run`, `FF_UPGRADE_FROM`/`FF_UPGRADE_TO` in env. NOT built — a new extension point is a new contract and earns its own premise review, exactly like the last three tickets: unanswered are whether *files changed AND the run failed* is a terminal state Flow will ship, its ordering against manifest re-stamp / pre-commit reinstall, its FR-27 bound (a consumer gate that hangs hangs the upgrade), and the M17 threat-model change of executing consumer code inside Flow's upgrade path. Structurally blocked meanwhile by `upgrade-sh-at-module-ceiling` — 821/800 lines, next change must decompose FIRST. Their interim manually-run composed gate works; not urgent |
| tool-time-protected-path-intent | filed, NOT built — E4 ask (b) shipped instead; (a) is the optional upgrade | Consumer proposal E4 ask (a). The deadlock is REAL: `has_active_exception` needs a `tree_digest` over CURRENT STAGED content for a digest-bound category, `pre_tool_use` evaluates the `Edit` BEFORE that content exists, and `write-bootstrap-approval.sh` exits when no category path is staged — so the first `Edit` of a protected path cannot mint the approval that would permit it. **Ask (b) SHIPPED** (`workflows/violation-recovery.md` § FR-07: tool-time arm is defense-in-depth for already-staged content, commit gate is the boundary, sanctioned path is stage → mint → commit → `--consume`, disclosed in the commit body). Ask (a) — a `protected_path_intent` artifact, one exact path, one tool set, ≤10 min TTL, consumed on use, writer+verifier sharing ONE `path_policy` constants source (their ask 3, the part worth keeping) — is NOT built: it is a FOURTH artifact carrier on a gate whose carrier model is the open question (`carrier-aware-approval-binding` + `compat-approval-surfacing` both require the carrier table FIRST; three surfacing rounds already failed three reviews on exactly that). **Found while filing, named in neither proposal:** `post_tool_use.py:41-50` re-evaluates the same path after the Edit, so an intent consumed at `pre_tool_use` makes it report `exception=False` for the edit it just authorized — a false FR-07 warning and a misleading audit row. Good news also verified: `permission_request.py:48` handles COMMAND tools only and never evaluates an Edit path, so the double-evaluation defect that killed claim-at-evaluation for commands does NOT apply here. Residual with (b) documented is ergonomics only |
| wire-hooks-skips-occupied-pretooluse | open — MEASURED defect, filed NOT fixed | **Found while filing E5; the consumer never reported it** (their mint-then-trim keeps upstream's PreToolUse object, which routes around it). `--wire-hooks` NEVER adds Flow's PreToolUse handler to a `.claude/settings.json` whose `PreToolUse` array is already non-empty — it exits **rc 0** reporting *7 changes applied* while `grep -c hooks/handlers/pre_tool_use.py` on the merged file is **0** (OBSERVED, deterministic, probe `/c/tmp/e5-probe`; control cases: key-absent → wired, `[]` → wired, consumer+flow → present, **consumer-only → NOT wired**). Mechanism: the add branch `if event not in hooks or not hooks[event]` is a WHOLESALE REPLACE, correctly guarded to fire only when nothing needs preserving — but there is no add-beside-preserving branch for PreToolUse, only Stop has one. Worse than a missing feature: `post-fusebase-update.sh:377-385` records the wiring-intent marker on that rc 0 (`hook-wiring-intent.sh:76-83`), so the tree records INTENT while enforcement is absent; the health arm then correctly reports *PreToolUse ENFORCEMENT STRIPPED* and prints the recovery `--wire-hooks` — **the command that just produced the state**, a loop that cannot converge. `live-enforcement-inertness` class: wired-looking, content-healthy, not running. Fix needs a decision first — (A) add-beside-preserve mirroring Stop, plus (B) never record an intent that was not achieved, which is the same predicate as E5 ask 1; resolve with `enforcement-only-hook-wiring` |
| fingerprint-row-driven-by-publish-not-tag | fixed — enforced by preflight (2026-08-14) | Consumer finding N3: `v4.9.2`'s published tree ends its fingerprint table at `v4.9.0` — `v4.9.1` was tagged well before `v4.9.2` was cut, so its row COULD have shipped inside that tree and did not. Mechanism: `PUBLISHING.md`'s append step sat under "After publication" and ran only after a SUCCESSFUL publish, so a red release skipped it and the next release only remembered the tag it superseded — **two consecutive reds silently drop one row**, which is what v4.9.0+v4.9.1 (both RED) did. Tags are permanent, so the miss is unrecoverable for an adopter holding either tree. Fix: drive the assertion off the TAG, not the release run — `hooks/local/preflight.sh` fails when any `v*` tag in the table's coverage window has no row (exemption: a tag at HEAD, which cannot contain its own row), naming the tag and the generating command, so a missed row BLOCKS the next cut. The check found a third instance on its first run: `v4.7.1` never got a row at all. RED-first proof drives the real `v4.9.1` deletion (`hooks/tests/test-fingerprint-row-per-tag.sh`). Exposed by the two RED Windows runs of `release-gate-flaky-job-probe` |
| release-gate-flaky-job-probe | CLOSED (2026-08-14) — bound raised, confirmed on three hosted runs | `msys-tree-cleanup ws2hard-probe-gating` failed then passed on the **same SHA** `20fd707` during the v4.8.0 publication (release run `31459937396` attempt 1 red, attempt 2 green; run `31459050444` green 40 min earlier). 928/929 — one powershell-backed capability probe decided publication. The release gate is non-deterministic, and the sanctioned remedy for a transient failure is a re-run (`PUBLISHING.md:255`), so the habit it trains is "re-run until green" — precisely how a real failure gets waved through. Third recorded instance of the verdict being set by ambient host conditions rather than code, alongside `gate-bounds-lack-headroom` and `harness-kill-leaves-orphan-children`. Reproduce before touching the assertion: `test-msys-tree-cleanup.sh:376` deliberately refuses to skip when the mechanism cannot run, so relaxing it would delete real coverage. **DIAGNOSED (occ. 5, run `31753213227`):** the instrumented probe emitted `rc=124 elapsed_ms=15271 helper=ok marker=absent detail=` — PowerShell cold start on a LOADED hosted runner exceeds the probe's 15s watchdog by 271ms and produces no output at all. Bound raised to **46s** (~3x the measured loaded-host worst case, per `gate-bounds-lack-headroom`; deliberately not a rounded-up observation). Helper-path failure and the partial-write TOCTOU are EXCLUDED by that same line; the atomic-publish fix shipped anyway as correct-but-unrelated |
| upgrade-source-integrity-and-observability | done (v4.7.0 — published 2026-08-04) | Upgrade-path source integrity from a consumer report that needed seven manual interventions: incoming managed content is materialized from **git objects** (never the staging worktree, closing the permanent Windows `FLOW_LAYER_DRIFT`), one source boundary in `hooks/local/lib/materialize-managed-source.sh`, `bootstrap-upgrade.sh --repair-managed <path>` for the already-corrupted file, sanctioned `cleanup-flow-backups.sh` replacing the raw `rm -rf` the guard denies, parent-owned heartbeat on captured long runs, and `created_at` + verdict-neutral stale-approval warnings. The repair-verification CONTRACT is decided, not patched: **M13** binds the manifest layer set at authorization (before any pre-existing file is touched; `rc == 0` AND parsed exact `MATCH` per bound layer), **M16** declares membership from the VERIFIED upstream tree (M14 refuted), **M17** locks the same-principal threat model. Deploy hash `bad4d92`; 8 review rounds; process lesson filed as `undecided-contract-drives-repeat-defects`; hostile-co-tenant case parked as `repair-trust-root-outside-workspace` |
| approval-binding-and-upgrade-classification | done (v4.7.0 — published 2026-08-04) | FR-12 command gate no longer replayable: schema v2 (parsed mandatory expiry, body/filename action agreement, type-safe fail-closed), command-digest + repo binding, all-matching-rules denials naming every required action, SQL/`rm`/Lightweight-lane gate fixes, safe `approve-local.sh` writer + `--inventory` (strict OFF this release, K7), and a managed-content base manifest driving 10-state upgrade classification so a consumer's local patch is preserved and reported instead of silently overwritten. Deploy hash `6224bbe`; smoke 6/6; known limit K21 (regex evasion) tracked in `command-gate-shell-evasion` |
| approval-single-use-consumption | parked — host call identity absent | True one-shot approval consumption (reserve -> execute -> consume). Deferred from `approval-binding-and-upgrade-classification` (K11): needs a stable host call ID across both hook entry points, success/failure finalization, orphan TTL, and Windows + network-FS atomicity. Binding (K2) covers most of the replay risk meanwhile |
| rm-rule-pattern-single-space-gap | done (v4.7.0 — published 2026-08-04) | CLOSED by `approval-binding-and-upgrade-classification` T28: pattern is now `(?<![-\w])rm\s+(-[A-Za-z]+\s+)*\S`, so `rm build.log` / `rm -f build.log` gate `destructive_file_delete`; `docker run --rm image` stays out |
| repair-trust-root-outside-workspace | not-planned under locked threat model | `--repair-managed` anchors its bound layer set on the VERIFIED staging tree (M16), which is outside the **repaired tree's** control but not outside the workspace's: whoever can commit into `.fusebase-flow-source` and move its ref can author a self-consistent tree declaring fewer layers. Accepted under M17's same-principal threat model; this is the hostile-co-tenant case, needing a trust root Flow does not have — remote fetch at repair time or signature verification (K3: no signing seam). Cross-linked to K3, M17 and `provenance-and-single-seam-guarantees` |
| command-gate-shell-evasion | active — semantic corpus + decision only (no parser patch) | Rule matching is regex over the RAW command string, so `fusebase de'pl'oy` and dynamically constructed commands execute ungated (K21: documented, not half-fixed — the real fix is shell-aware parsing or a conservative deny, both with false-positive risk) |
| rule-inventory-version-literal-noise | done (v4.7.0 — published 2026-08-04) | `norm()` normalizes `v<major>.<minor>.<patch>` → `vX.Y.Z`, so a version-only release bump leaves the rule-loss tripwire's diff EMPTY while an attestation reword still goes RED; 170 rows unchanged. Lightweight lane — [`docs/changes/2026-07-27-rule-inventory-version-literal-noise.md`](../changes/2026-07-27-rule-inventory-version-literal-noise.md) |
| upgrade-sh-at-module-ceiling | open — a DEBT WITH A NAMED PAYER, not an amnesty | `hooks/local/upgrade.sh` was **794/800 before** `n5-upgrade-silent-no-op` — it is at the FR-25 ceiling as a STANDING condition, not as a consequence of that ticket (N5 added 27 lines of call sites; both its new concerns were already extracted into libs). Baseline re-keyed to 821 for that ONE path, for N5 only, **to keep an urgent live consumer fix separable and revertable** — a field rollback must be "the N5 guard", not "the guard plus an engine reshuffle". Adopting a baseline resets the ratchet reference point, which is the creep FR-25 exists to prevent, so: **the next change touching upgrade.sh must decompose it first — prerequisite, not cleanup.** Seam already identified so the next agent inherits the analysis: `print_recovery_hint` + `ffhc_run_step` (bounded step execution + recovery messaging, already lib-adjacent via run-with-timeout.sh, ~60 lines), with the two constraints that bind it |
| stamper-hashes-worktree-not-artifact | open — needs a DECISION, not an implementation | The stampers hash WORKING-TREE bytes, so a file whose worktree copy differs from what git materializes (the `.gitattributes` eol case) makes the manifest describe bytes that never ship — undetectable locally by construction, since the stamper and the verifier read the same wrong bytes and agree. Occurrence: `audit/hook-layer-manifest.json` recorded `4145ce9c…` while the blob hashed `b084434258f3970e`; hosted RED both platforms at `22873d6`, fixed `b57c62f`. **NOT a free improvement** — hashing committed content instead would trade away the tamper-detection property the health check's integrity critical depends on (`mutable-python-load-point`). Four options costed; no recommendation recorded on purpose |
| manifest-drift-attribution | filed, NOT built — excluded from default `verify` by premise review (2026-08-18) | `DRIFT` names files; the operator reconstructs *why* by hand. Real forensic value, measured: a consumer's 9 modified files spanned **five** later commits rather than the one assumed, and **2 had no post-stamp commit at all** because the manifest had recorded CRLF bytes — a correct hold, not a wrong re-stamp. Kept OUT of default `verify`: git history is read-only so it breaks no mutation contract, but it changes dependency (non-Git trees), correctness (shallow history makes "no post-stamp commit" FALSE), anchor validity (an uncommitted manifest misplaces the anchor) and cost (one `git log` per drifted file). Lands behind `verify --explain`, a standalone command, or a post-DRIFT health recommendation, with three hard requirements: one batched history query; never move the verdict or exit code when attribution is unavailable; never claim "byte-level divergence" unless the manifest is tracked+clean, the anchor exists, and history is known complete. **Wasted-work prediction recorded before any code:** in the critical verifier it is removed within three months |
| local-gate-misses-manifest-freshness | built + proven, NOT shipped — placement is an open operator decision | The local full suite has no manifest-freshness assertion but CI does — a missed `stamp-hook-manifest.sh` passes locally and reddens main (caused `eca925b`). Built in `cli-0298-compatibility`, rewritten non-mutating (scratch `git worktree`, no blanket `worktree prune`) after the 2026-08-05 review's **point 3 was UPHELD**, and proven by hosted CI **GREEN both platforms 1094/1094 zero skips** at `2e9315d` — where it caught a published integrity artifact recording a digest of bytes that never ship. **Removed from the shipped gate anyway**: `b57c62f` fixed that defect and its root cause independently, so the phase was credited with a benefit belonging to another commit. Code + evidence + adoption steps preserved at `s9-manifest-fresh/`; what is lost is local pre-push detection, not the guard (CI still catches it) |
| install-into-existing-fusebase-cli-project | automation parked; doc audit active (T9) | Safer installer for repos that already have FuseBase CLI and MCP configuration |
| architect-sub-agent | not planned — no measured PO context-cost defect | Dedicated 3rd sub-agent for deep cross-cutting investigation, keeping the PO lean |
| role-path-hook-enforcement | not planned — claimed guarantee is fail-open | `pre_tool_use` role × path gate — PO "no app code" as a structural guarantee (FR-25 plumbing precedent) |
| health-check-fast-timeout | done (v3.24.0) | Bound the health-check's unbounded git-fetch + sub-script invocations so it never appears to hang (timeouts + partial verdict + `--fast` mode) |
| upgrade-tooling-hardening | done (v3.25.0) | Harden the refresh/upgrade scripts from two consumer reports — Windows perf (batched spawns), EOF-newline preserve, baseline/policy merge-preserve, sync allowlist + under-reach guard, GEMINI version sync, `PARTIAL_UPGRADE` |
| upgrade-baseline-bootstrap-hop | done (v3.25.1) | Hotfix — the v3.25.0 U3/W2 baseline merge-preserve now runs on the FIRST upgrade adopting v3.25.x (merge lib sourced from authoritative target tree + re-source before Step 1a + loud no-skip warning; `bootstrap-upgrade.sh` stages `hooks/local/lib/`; README routes pre-v3.25 installs through bootstrap; RED-then-GREEN adoption-hop test). Found by post-ship Codex adversarial review (`ea85585`) |
| adapter-overlay-refresh-parity | reopened | U6 follow-up — marker-anchored overlay-refresh path for GEMINI/copilot/cursor (parity with AGENTS/CLAUDE), so secondary adapters get the Flow overlay content refreshed, not just the version string |
| fr22-delivery-guarantee | done (v3.27.0) | FR-22 delivery guarantee — present-by-construction handoff block + write-time digest non-propagation note + `comment_policy_review_applied` warn-signal + artifact-vs-content distinction doc + carve-out clarity (all artifact-level, never gating comment content). Phase 2/F deferred to `fr22-predelegation-hook` |
| po-verifiable-boot | done (v3.27.0) | PO verifiable boot — `/product-owner` activation checklist + ASCII `PO-ACTIVATED` marker (command + canonical agent) + warn-only Stop verification on the already-wired UserPromptSubmit/Stop events |
| fr22-predelegation-hook | not planned — no observed escape | FR-22 Phase 2/F — pre-delegation PreToolUse check that a code-writing sub-agent launch carries the FR-22 push block; needs host-matcher coverage (Task/Agent tool), explicit delegation markers, warn-only telemetry first (inert under shipped matchers as originally written) |
| liveness-discipline | done (v3.28.0) | FR-27 liveness discipline — never launch long/silent background work bare; new always-on rule + 32nd skill `liveness-discipline` + `hooks/local/lib/bounded-run.sh` structural watchdog (reuses health-check timeout core, ffhc_* intact) + 3-tier present-by-construction delivery. Honest model: no blocking gate, no verification hook (a hang is undetectable by construction). Deferred to `fr27-prelaunch-nudge` |
| fr27-prelaunch-nudge | not planned — warning prose is not liveness | FR-27 follow-up (D3) — warn-only `pre_tool_use` pre-launch nudge for a background launch with no liveness guarantee; must ship as **allow + warning** (never a block: `_output_decision()` maps non-allow/non-deny → interactive `ask`). Notes smaller siblings D4 (Python watchdog helper) + D5 (`templates/bounded-script.sh` skeleton) |
| codex-slash-command-parity | done (v3.29.0) | Codex / cross-agent slash-command parity — B (primary, repo-portable) a 6-row command-equivalents table in `AGENTS.md` + overlay so all 6 Flow commands work across Codex/Cursor/Copilot/Gemini via the portable convention; A (optional, opt-in) `hooks/local/install-codex-prompts.sh` single-sources native Codex `/prompts:<cmd>` (per-machine, namespaced, Codex-deprecated, marked, collision-safe, idempotent, frontmatter-less hard-fail). No FR/skill added. Follow-up: `codex-plugin-packaging` |
| codex-plugin-packaging | shipped (v4.4.0) | Codex plugin wrapper added via `.codex-plugin/plugin.json`, pointing at the existing `.agents/skills/` surface; `product-owner` is now a Codex-visible skill bridge. Plugins still do not carry custom slash commands, so `/prompts:<cmd>` remains optional legacy polish |
| cli-0.25.9-vendor-refresh | done (v3.30.0) | FuseBase CLI 0.25.9 vendor refresh + de-staled CLI-Stop-hook model — preserve-only merge (appends stop.py, never re-injects run-typecheck-apps.js, never removes) + diff-framed health-check (flags only a genuinely-dropped CLI Stop hook; no false SHARED_MERGE_DRIFT on 0.25.9's 3-hook set); full re-vendor to 0.25.9 (20 provider skills incl. flag-gated app-api-contract-testing, 4 hooks, 2 agents; manifest re-stamped); settings.json.example aligned to the 0.25.9 wired set; new compat suite (12) + recovery test updated. No FR/Flow skill added (the 20 is the CLI-provider count). Follow-ups: `healthcheck-diff-source-hardening`, `cli-custom-at-risk-overflag` |
| healthcheck-diff-source-hardening | done (v3.30.1) | RESOLVED by the advisory-only durable-receipt model — durable updater-written `state/audit/cli-stop-baseline.json` replaces the ephemeral `.pre-flow-merge` diff source (closes the no-op blind spot); a dropped CLI Stop hook → advisory `CLI_STOP_BASELINE_DRIFT`, an absent receipt → advisory `CLI_STOP_UNVERIFIED` (never silent); the v3.30.0 `SHARED_MERGE_DRIFT`/exit-1 path removed (preserve-only merge ⇒ a missing hook is never a merge fault); no on-disk-but-unwired fallback (run-typecheck-apps.js stays benign) |
| token-floor-remediation | done (v4.6.0, published in v4.7.0 2026-08-04) | Session boot-floor remediation from a consumer `/token-waste-audit` escalation — role-aware floor **78,148 → 41,321 bytes (47.1%)** with **zero rule loss** (residency-aware `rule-inventory.sh`, 170 rows, + `test-prohibition-residency.sh`); amendment log extracted to root `FLOW_RULES_HISTORY.md` behind a compatibility `## Amendment log` stub; exact-body anti-reread rule with the per-surface auto-load matrix corrected (descriptions inject, **bodies do not** — narrows the original "double-pay" premise); delegated chat-return budget ≤80 lines AND ≤6k characters (gate/deploy reports exempt); supersede-vs-write-primitive stated without contradiction; `token-waste-audit.py` labels rather than hides (verb-anchored probe match + path canonicalization); FR-27 delegate retry bounded to 3 attempts / 5 min. The escalation's ≤5k-token ask was arithmetically impossible (operative `FLOW_RULES.md` alone was ≈5k tok) — the honest 47.1% is what ships, and lazy `references/` content is **re-tiered, not deleted**. Gate 28/28 AC, 619/619 tests; six BLOCKERs were found by adversarial review *after* the first gate passed. [`docs/specs/token-floor-remediation/`](../specs/token-floor-remediation/spec.md) |
| cli-custom-at-risk-overflag | done (v3.30.1) | RESOLVED by the sha-gate — `CLI_CUSTOM_AT_RISK` now fires only on provenance drift (sha ≠ provenance); pristine CLI-shipped CUSTOM blocks (e.g. `app-dev-practices`) no longer over-flagged; genuine operator/Flow at-risk signal preserved (provenance-absent → conservative flag); advisory-only contract unchanged |
| m19-recovery-hint-honesty | done (v4.7.1 — published 2026-08-05) | The M19 residual, split out of `compat-approval-surfacing` and shipped alone: `upgrade.sh`'s recovery block no longer promises engine continuity in ANY carrier — the printed prose (M19, v4.6.x), the trailing `# re-run; completes remaining steps` on the first command, and the `main()` header comment are all gone, deleted rather than qualified. Four assertion names in `test-recovery-hint-honesty.sh` narrowed to what their predicates actually check (or the predicate widened to the name): one shared `CONTINUITY_RE` family now guards both carriers, so the two cannot drift apart again. Discriminator: against `02d14f7` the old test is 6/6 green while the widened test is 4/6, catching the exact literal it missed. Defect class — **a claim wider than the thing it describes** — recurred inside its own fix and was caught by review (3 findings, all class (a)). Deploy hash `3ae1feb`; Linux 746/746; Windows 718/721 with **two watchdog crossings ratified**, not clean — see `gate-bounds-lack-headroom` |
| harness-kill-leaves-orphan-children | active — S2 (reproduction first) | A gate killed by an EXTERNAL signal (outer `timeout`, or operator Ctrl-C) leaves its bounded child alive: three `test-cli-flow-recovery.sh` processes survived **38 minutes** past the parent, one spawned AFTER the parent died. The reaper works on a clean exit (a completed 768/768 run left zero processes) — the gap is the signalled path `_ff_exit_reap` says it covers. Matters because this is how the repo's documented "competing suite corrupts timings" condition gets created: the next run's phase times are inflated by an invisible cause and read as a flaky test. Can corrupt the very evidence `gate-bounds-lack-headroom` is diagnosed from. Needs a red arm (kill mid-phase, assert zero survivors) or it cannot be distinguished from "it did not leak this time" |
| gate-bounds-lack-headroom | active — S3A/S3B (instrument first) | Bounded test phases sit within **0.5%** of their watchdog walls on an ordinary developer host, so the gate's verdict is decided by ambient load rather than by the code: `cli-flow-recovery` 903s/900s, `bootstrap-exception` 602s/600s, `upgrade-repair-managed` 603s/600s — all exit 124, **zero assertions failed**, Linux 746/746 on the identical commit. Ratified as non-blocking for v4.7.1 rather than waived. The rule it needs: **a bound is a liveness backstop, not a performance assertion** — deliberate headroom (2–3×) over a *loaded-host* worst case, never a rounded-up clean-host observation (M15 warned this in writing and it came true three releases later; `cli-flow-recovery` has now crossed four times across 240s → 480s → 900s) |
| compat-approval-surfacing | parked — carrier table absent | Surface K7 compat-accepted approvals at upgrade + health (consumer measured a live open deploy gate: 98 artifacts, several expiry-less, one satisfying `production_deploy` permanently). **Three implementation rounds failed three adversarial reviews, each round's fix causing the next round's defect** — the unspecified contract is that "judge as the gate judges" has no single answer (`command_policy` supplies `repo_id`+`command_digest`, `path_policy` supplies neither, health deferrals aren't in `require_approval`). Needs a **carrier table** designed before any further implementation. Full history on tag `parked-v471-surfacing`. **M19 residual CLOSED** and shipped separately in v4.7.1 (`m19-recovery-hint-honesty`, `3ae1feb`) — the surfacing feature itself remains parked and unstarted |
| self-granting-health-deferral | reopened | A `deferred_checks` entry containing a newline splits into two bash entries, letting an artifact grant itself a deferral for a canonical `check_id` it never carried — moving the health verdict to `EXCEPTION_IN_EFFECT` and exit 3. Pre-existing (`382a05e`), observed not reasoned. Fix is **validate-and-reject** (full-match the charset, never repair — deletion manufactures canonical ids) + NUL-delimited transport. Independent of `compat-approval-surfacing` |
