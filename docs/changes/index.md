# Lightweight-lane ledger (FR-21 telemetry)

One line per Lightweight-lane ticket, plus one line per mid-flight promotion. This is the minimal traceability the Lightweight Lane keeps (see `skills/lightweight-lane/SKILL.md`). It is a ledger, not a report — keep entries to one line.

Format:

```
<YYYY-MM-DD> · <slug> · lightweight · <commit SHA>
<YYYY-MM-DD> · <slug> · promoted lightweight→full · <reason>
```

## Entries

<!-- newest first -->
