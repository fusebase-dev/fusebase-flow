change_tier: lightweight
ticket: v3181-nit-sweep

Problem:   Independent post-ship audit of v3.18.0 (the diff had no pre-ship
           reviewer): 0 blockers, 9 nits + 3 pre-existing finds. Worst: the
           waiver-path DP.6 prompt claimed "Approval artifact verified" before
           the artifact exists on eligible deploys; the ai-developer agent's
           gate-field restatement had ALREADY drifted (missing
           implementation_summary) — proving the restatement hazard; nothing
           told the PO to write the literal "no real decisions" line that
           required-artifacts optional_when consumes.
Change:    N1 handoff-deploy DP.6 prompt branched (verified | waiver-eligible,
           stamp-on-phrase) + stamp step in the response line; N2 read 7
           waiver caveat; N3 unescaped pipe in dp1_waiver row; N4 agent 6c row
           + IM.9 -> pointers at gate-contracts.yml/gate-report template;
           N5 README :230/:334 conditional security review; N6 carrier count
           5->7 in v0.23 amendment + CHANGELOG (same-release factual fix);
           N7 task_complete removed from flow_hook_event.schema.json enum
           (.codex example verified not wiring it); N8 release-notes 5e->5d;
           N9 release-deploy-reporting step 1 conditional + step 4 ->
           templates/handoff-deploy.md canonical; producer gap:
           implementation-planning decisions row records the literal
           "no real decisions" in spec.md; + same-class step 12 ->
           templates/handoff-implement.md; gate-report placeholder attestation
           now sweep-maintained ("under Fusebase Flow vX"); required-artifacts
           header consumer corrected (stop.py only).
Verified:  preflight 0/0; run-tests 24/24; --all 0; sweep clean; schema valid
           JSON; mirrors re-run 0 drift.
Rollback:  git revert <SHA>
Commit:    a0cd536
Deploy:    operator "proceed to fix all of them" -> v3.18.1 tag, push
           origin main --follow-tags.
