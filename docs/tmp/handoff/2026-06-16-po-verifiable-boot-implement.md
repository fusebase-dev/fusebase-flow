# Implement handoff — po-verifiable-boot

## Role bootstrap
You are the **AI Developer** under FuseBase Flow v3.26.0. Self-attest FR-01..FR-26 + IM.1..IM.18. Load-bearing: FR-03 (one task=one commit), FR-05 (stop at gate), FR-07 (worker-undisturbed), FR-13 (lint/typecheck per commit), FR-22 (comments — block below), FR-25 (module size). **Run synchronously; stop at gate; do NOT bump version, push, or deploy.**

## COMMENT POLICY (FR-22) — applies to all code you write
```
Write ONLY two kinds of comment; remove everything else.
1) TRIPWIRE — a constraint an editor could break unknowingly, not obvious from local code (≤1 line; ≤4 lines only for security/auth/concurrency/platform).
2) RETRIEVAL POINTER — a ≤1-line tag naming the external WHY-home, e.g. "(decision D1)".
REMOVE: comments restating the code; rationale already in a decision/ticket/memory; changelog/history (it's in git).
Do NOT match surrounding comment density upward. Keep pointers — not duplicates.
```
At done, emit in chat: `comment-policy review: applied (FR-22)`.

## Mandatory reads
1. `FLOW_RULES.md` FR-01..FR-26 (stop at Amendment log).
2. `docs/specs/po-verifiable-boot/spec.md` — LOCKED. Decisions D1–D5, Tasks T1–T5, ACs are consolidated there (FR-23). Authoritative.
3. The surfaces you edit (read each first): `.claude/commands/product-owner.md` + `hooks/local/fusebase-flow-overlays/commands/product-owner.md`; **canonical** `agents/product-owner/AGENT.md` (NOT the `.claude`/`.codex` mirrors — those are generated by `mirror-agents.sh`); `hooks/handlers/user_prompt_submit.py`; `hooks/handlers/stop.py`; `policies/required-artifacts.yml`.
4. `hooks/local/mirror-agents.sh` (how agents mirror) and `audit/agent-mirror-manifest.txt` (preflight checks agent drift).
5. `flow-skills/role-discipline/references/ai-developer.md`.

## Scope — T1–T5, one commit each. Warn-only everywhere; never deny.

- **T1 (A) — command boot block.** In BOTH `.claude/commands/product-owner.md` and `hooks/local/fusebase-flow-overlays/commands/product-owner.md` (keep byte-identical): replace step 1's prose self-attest with a **present-by-construction 6–8 line activation checklist** the PO must ECHO as its first reply, ending with the **ASCII-only** marker (the PO substitutes the live VERSION it read at session start):
  ```
  [[ PO-ACTIVATED | FuseBase Flow <VERSION> | FR-01..FR-26 | no-app-code | lane-first | operator-locked-decisions | approval-gated-deploy | context:<north-star|generic> ]]
  ```
  Checklist lines cover: advise+plan / NO app code (FR-01) · lane-first (FR-21) · lifecycle Specify→…→handoff · decisions operator-locked · questions in chat (FR-19) · deploy approval-gated · Mode A/B + pointers-over-re-paste (FR-23/26) · read North Star if onboarded. Pointers to FLOW_RULES — NO re-paste.
- **T2 (B) — canonical agent + mirror.** Edit canonical `agents/product-owner/AGENT.md` to carry the SAME boot block + marker (so the PO boots whether via slash command or Agent-tool/description spawn). Then run `bash hooks/local/mirror-agents.sh` to regenerate `.claude/agents/product-owner.md` + `.codex/agents/product-owner.md` + the manifest. Do NOT hand-edit the mirrors.
- **T3 (C) — UserPromptSubmit reminder.** `hooks/handlers/user_prompt_submit.py`: detect a `/product-owner` invocation in `event.user_prompt` → add a one-line non-blocking reminder ("PO session — emit the activation boot + marker first") to the warnings path + a supplemental `po_activation_requested` audit field. Warn-class, never blocks. Match the existing detector/emit style.
- **T4 (D) — dedicated PO-activation Stop path.** `hooks/handlers/stop.py`: add a path that runs **independent of the done/deploy `CLAIM_PATTERNS` gate** (the existing recommended-list only fires after a claim phrase — a normal PO first reply has none). Logic: scan the transcript; if it shows a `/product-owner` activation (literal `/product-owner` input OR the `PO-ACTIVATED | FuseBase Flow` marker) AND the marker is ABSENT → emit a **warn** (via `emit(...)`/stderr) and still **allow** (rc 0); never touch the done/deploy decision. Detection by literal transcript scan ONLY — no audit-log session correlation, no rule-content inspection. Add `po_activation_attested` to `policies/required-artifacts.yml: signal_definitions` for documentation (its firing is the dedicated path, NOT a `before_done_claim` entry — do not add it there).
- **T5 — tests + re-mirror.** `hooks/tests/`: (AC1) both command copies contain the block + marker template (match the stable prefix `[[ PO-ACTIVATED | FuseBase Flow`, not a version number) and are byte-identical; (AC2) command boot block == agent boot block (drift guard); (AC3) user_prompt_submit detects `/product-owner` → reminder, and a non-PO prompt does NOT (no false positive); (AC4) RED-then-GREEN on the stop.py path — activation-without-marker → warn+ALLOW, with-marker → no warn, non-PO transcript → no warn, AND a done/deploy claim missing a REQUIRED signal still DENIES (prove the gate isn't loosened, and prove the PO path fires WITHOUT a claim phrase). Wire into `run-tests.sh`. Re-mirror agents (already in T2) — confirm 0 drift. Genuine RED-then-GREEN, loud asserts, no false-green.

## FR-07 / protected paths
Check `policies/protected-paths.yml`; hooks are opt-in here (no `.claude/settings.json`) so no approval artifact is mechanically required — honor the spec regardless. Do NOT diff the `FLOW_RULES.md` FR rule rows, the 3 deploy-policy rule semantics (`approval-policy.yml`/`protected-paths.yml`/`command-policy.yml`), or `ratchet-governance.yml`. In-scope edits: the command (+overlay), canonical agent (+mirrors), `user_prompt_submit.py`, `stop.py`, `required-artifacts.yml` (signal_definitions add only).

## Per-commit pre-attestation
```
☐ preflight 0/0  ☐ worker-undisturbed unchanged  ☐ one task scope  ☐ no TODO/FIXME/WIP
☐ FR-22 comments in any code  ☐ FR-25 <ceiling  ☐ warn-only (no new deny)
☐ FLOW_RULES FR rows / 3 deploy-policy semantics / ratchet UNCHANGED  ☐ commit cites the task
```

## Gate (stop, report, HALT)
preflight 0/0 · `python -m py_compile hooks/handlers/stop.py hooks/handlers/user_prompt_submit.py` · run-tests PASS incl. new tests · check-module-size --all exit 0 · mirror 0 drift (agents byte-identical + manifest) · the 4 FR-07 surfaces unchanged. Produce the gate report; HALT. Emit `comment-policy review: applied (FR-22)`. A Codex impl review runs after the gate.

## Return
Gate report: per-task SHAs (T1–T5), AC1–AC4 evidence (esp. AC4 RED-then-GREEN proving the PO Stop path fires WITHOUT a claim phrase AND required-missing still denies), gate numbers, FR-07 confirmation (4 surfaces unchanged), and the dogfood marker.
