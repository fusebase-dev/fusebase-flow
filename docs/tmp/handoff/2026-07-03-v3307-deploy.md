# Deploy handoff — FuseBase Flow v3.30.7 (Phase C — Fable whole-system audit fixes)

## Role bootstrap
AI Developer in **DEPLOY PHASE** under FuseBase Flow v3.30.6→v3.30.7. Self-attest FR-01..FR-27 + IM.1..IM.18 + DP.1..DP.12. Production release. Work SYNCHRONOUSLY; do NOT spawn background monitors; if a network op fails on a transient, STOP and report exactly where you are. **If you hit a pre-commit gate you cannot resolve within your role (FR-25 module-size, protected-path, etc.), STOP and report the EXACT blocker + recommended resolution in your FIRST response — do NOT stall silently.**

## Deploy authorization (DP.1 / DP.6)
- Operator STANDING full authorization for this autonomous roadmap run (operator away; no real data/users on prod). This is Phase 3 (Fable audit + fixes) of the 3-phase mandate. DP.6 equivalent = the standing authorization.
- MINT the DP.1 artifact `state/approvals/production_deploy-v3307-20260703.json` (scope: v3.30.7 Phase C audit fixes; HEAD eb50078; unexpired) before the release commit. (state/approvals/* is gitignored local state.)

## Release candidate
- HEAD to release: **`9790c90`** (Phase C S1b) — the v3.30.7 candidate: S1 `4acb535` (hook host-compat) + S2 `92135f4` (release-doc backfill) + S4 `9424df8` (fail-closed tooling) + S5 `1523a86` (po-investigate hardening) + S6 `eb50078` (doc/rule/path consistency) + S1b `9790c90` (stop.py fail-closed on transcript-extraction failure — deploy-gate review fix) on v3.30.6 `676f4b1`. **Verify HEAD == `9790c90`.**
- Phase C = an independent Fable whole-system audit (9 subsystems) → 40 findings → these 6 Opus-implemented fix slices. The consolidated adversarial review (Codex + a 3-lens Opus panel) confirmed: enforcement now FIRES on native Claude Code events without weakening any gate; fail-closed preserved; po-investigate tighter; docs-only sweep changed no enforcement; the v3.30.5 FR-07/FR-12 pre-commit chain (hooks/git/pre-commit, hooks/shared/**) is UNTOUCHED by the whole stack. (Confirm the review was SHIP before you start.)
- Re-verify `.git/hooks/pre-commit` == source. Branch `main`, remote `origin`. Current published: `82c90dc` (v3.30.6, tag v3.30.6).

## LOADED-HOST DISCIPLINE
CPU-saturated host. Pre-raise `export FF_CLI_RECOVERY_TIMEOUT=600 FF_PHASE_TIMEOUT=900`. Do NOT run the full `run-tests.sh` as deploy smoke (~40+ min, false-INCONCLUSIVE-prone; the stack was gated per-slice + reviewed). Deploy smoke = preflight + health-check + tag/release verification.

## Pre-deploy verification (do FIRST — abort on any failure)
1. `bash hooks/local/preflight.sh` → 0 errors / 0 warnings (NOTE: preflight now also parity-checks marketplace.json — confirm it's happy).
2. **SINGLE** `bash hooks/local/mirror-skills.sh --check` → 0 drift; `mirror-agents.sh --check` → 0 drift.
3. `audit/skill-mirror-manifest.txt`: raw == unique == **86**, 0 dups.
4. `git status` clean except intended untracked docs.
5. **Re-read `VERSION`** immediately before writing — must read `3.30.6`.

## Deploy steps (one release commit + one FR-14 docs commit)
1. **Bump VERSION** `3.30.6` → `3.30.7`.
2. **Bump `.claude-plugin/plugin.json`** `3.30.6` → `3.30.7` AND **`.claude-plugin/marketplace.json`** `plugins[0].version` `3.30.6` → `3.30.7` (S2 added marketplace.json to preflight §8 parity — both must match VERSION or preflight fails).
3. **Propagate**: `bash hooks/local/sync-version-strings.sh` ONCE; re-verify manifest 86/86/0-dups + SINGLE `mirror-skills.sh --check` = 0 drift.
4. **Release commit** (explicit paths, NOT `git add -A`; NO --no-verify) — commit-msg allowlist has no `release`, use: `flow(release): v3.30.7 — Phase C Fable audit fixes (live enforcement fires + fail-closed tooling + po-investigate hardening + release-doc/consistency)`. sync touches protected FLOW_RULES.md/plugin.json/VERSION → mint the sanctioned single-use bootstrap approval FIRST (`bash hooks/local/write-bootstrap-approval.sh`), commit, then `--consume`.
5. **Push** main. 6. **Tag** `v3.30.7` (annotated) + push. 7. **GitHub release** `gh release create v3.30.7` with notes:
   **Phase C — independent Fable whole-system audit (9 subsystems, 40 findings) → 6 Opus-implemented fixes. Headline: the Flow enforcement hooks now actually FIRE in live Claude Code sessions.**
   - (S1) **CRITICAL — live enforcement was inert.** The UserPromptSubmit + Stop handlers read Flow-schema keys (`user_prompt`/`agent_message`) that the Claude Code runtime never sends (it sends `prompt`/`transcript_path`), with no normalization shim — so the FR-12 pasted-secret warning, bypass detection, /product-owner reminder, and the FR-04/05/14 done/deploy-complete deny gate never fired on real prompts (the suite was green only on synthetic fixtures). Fix: handlers read the native shapes (dual-key `prompt`; parse the final assistant message from `transcript_path`) and surface warns via `hookSpecificOutput.additionalContext` + denies via stderr. **No gate weakened — the deny/warn logic is unchanged; only the input source was fixed.** New native-shape fixtures close the synthetic-only coverage gap.
   - (S4) fail-closed tooling: preflight skill-frontmatter + orphaned-approval checks now actually fail the exit code (a pre-`$?` `|| true` had made them false-clean); the health engine reads a completed run-tests with visible INCONCLUSIVE rows as UNVERIFIED (not false-BROKEN) while a genuine crash still BROKEN; verify-gate runs from any subdirectory.
   - (S5) the PO read-only investigation wrapper (`po-investigate.sh`) now refuses git write/exec escapes (`--output`, `--ext-diff`/`GIT_EXTERNAL_DIFF`, `-c *.external`, pager) + scrubs the env — the read-only guarantee is now structural.
   - (S2) backfilled the release-doc chain (CHANGELOG + release-notes for v3.30.3–v3.30.6, README/marketplace badges) + a marketplace.json version-parity preflight check.
   - (S6) doc/rule-text/path consistency sweep (the v3.9.0 `skills/`→`flow-skills/` rename residue, FLOW_RULES status/labels, rail-mapping hook-claims, catalog counts, dangling rule-IDs) — docs/text only, no enforcement change.
   - (S1b) the consolidated deploy-gate review (Codex) found + closed a corner-case in S1: the Stop done/deploy gate fell open when the transcript was corrupt/wrong-shape/format-drifted (the final assistant message couldn't be extracted) even though the raw transcript contained a done-claim. stop.py now FAILS CLOSED there (deny "could not verify — unverifiable transcript") without re-introducing over-trigger in the normal path. No worse than baseline before; now a hard-closed edge.
   The v3.30.5 fail-closed FR-07/FR-12 pre-commit chain is untouched (verified by both reviewers across the whole stack). 8. **Capture** the release commit SHA.

## Post-deploy — FR-14 SINGLE docs commit
One commit `docs(post-deploy): v3.30.7 Phase C audit fixes`:
- **Add a `docs/problem-catalog/` entry** for the **live-enforcement-inertness** lesson (the highest-value finding): a hook handler that reads a normalized/Flow-schema event field the host runtime never sends is INERT in production while its tests (which inject the schema shape) stay green — a synthetic-only coverage gap. Guardrail: test hooks with the HOST's NATIVE event shape (fixture the real `prompt`/`transcript_path`), not just the internal schema; a "green hook suite" does not prove the hook fires live. Cite S1 (`4acb535`).
- Optionally note the Phase C audit closeout (40 findings → 6 slices) in the roadmap/CHANGELOG.

## Smoke / probes (LIGHT)
- `bash hooks/local/fusebase-flow-health-check.sh` → capture verdict (PARTIAL_UNVERIFIED benign on the saturated host; preflight 0/0 is ground truth). Note: S4 fixed the false-BROKEN-on-INCONCLUSIVE path, so the verdict may be cleaner.
- `git tag --points-at <release-commit>` shows v3.30.7; `gh release view v3.30.7` isDraft=false.
- Consumer-verify (operator distributes to a quiet MINGW64 box): full `run-tests.sh` (incl. the new test-po-investigate + native-shape fixtures); confirm the live hooks fire on real Claude Code events.

## Hard rules
NO --no-verify. NO force-push. One release commit + one FR-14 docs commit. Re-read VERSION before writing. Bump plugin.json AND marketplace.json with VERSION. SINGLE mirror check. Manifest 86/86/0-dups before AND after sync. `hooks/git/pre-commit` + `hooks/shared/**` must remain UNTOUCHED. If anything is ambiguous or a gate blocks, STOP and report.

## Return
Deploy report: release commit SHA + tag + GitHub release URL, VERSION 3.30.7, plugin.json + marketplace.json 3.30.7, manifest 86/86/0-dups, mirror 0-drift, smoke/probe results, FR-14 docs commit SHA + problem-catalog path, DP.1 artifact used, consumer-verify items.
