# Implement handoff — cli-0.25.9-vendor-refresh

## Role bootstrap
You are the **AI Developer** under FuseBase Flow v3.29.0. Self-attest FR-01..FR-27 + IM.1..IM.18. Load-bearing: FR-03 (one task=one commit), FR-05 (stop at gate), FR-07, FR-13, FR-22 (comments), FR-25, **FR-27 (liveness — bound/poll any long run, never bare)**. **Synchronous; stop at gate; do NOT bump VERSION/push/deploy.**

## COMMENT POLICY (FR-22) — any code you write
```
ONLY tripwire + retrieval-pointer comments. Remove WHAT-restating/recorded-elsewhere/changelog. Don't match density upward. Keep pointers.
```
At done emit `comment-policy review: applied (FR-22)` (or N/A marker if no code diff).

## Mandatory reads
1. `FLOW_RULES.md` FR-01..FR-27 (stop at Amendment log).
2. `docs/specs/cli-0.25.9-vendor-refresh/spec.md` — LOCKED (D1=option C, D2=full re-vendor, D3=v3.30.0, D4 safety; ACs incl. AC3b/AC3c/AC3d). Authoritative.
3. The files you change: `hooks/local/check-cli-flow-conflicts.sh`, `hooks/local/fusebase-flow-overlays/settings-json-merge.py`, `hooks/local/fusebase-flow-overlays/agent-surface-ownership.json`, `.claude/settings.json.example`, `hooks/local/stamp-cli-provenance.sh` (+ its output `audit/cli-vendor-manifest.json`), `hooks/tests/test-cli-flow-recovery.sh`, docs.
4. The 0.25.9 CLI source — **re-extract from the zip to be safe**: `unzip -q "/c/Users/abcpa/OneDrive/Downloads/apps-cli-main (6).zip" -d /c/tmp/apps-cli-0259` → assets at `/c/tmp/apps-cli-0259/apps-cli-main/project-template/.claude/{skills,hooks,agents}`. Verify the flag name for `app-api-contract-testing` in the CLI's `copy-template.ts` (spec says `cross-app-api-calls-analysis`).

## Scope — one commit per task. Everything WARN/advisory-preserving; no new deny; read-only health-check stays read-only (no `fusebase` calls).

- **T1 (B + H1) — de-stale the CLI-Stop-hook model + fix the example.**
  - `settings-json-merge.py`: make CLI Stop handling **preserve-only** — append Flow `stop.py` once (idempotent) + keep every existing Stop hook; **remove `run-typecheck-apps.js` from the auto-inject `CLI_STOP_HOOKS`** (or gate any re-inject to "already wired"). NEVER remove an existing hook (older-CLI projects that still wire `run-typecheck-apps.js` keep it). Keep `enabledMcpjsonServers` preserve/merge as-is.
  - `check-cli-flow-conflicts.sh`: replace the hardcoded `expected_cli_markers` (~L338) with the **diff framing (D1c)** — flag `SHARED_MERGE_DRIFT` ("CLI Stop hook not preserved: X") ONLY when a CLI Stop command present in the **pre-merge `.claude/settings.json`** Stop chain is missing after Flow's stop.py was added. Determine "CLI command" by `.claude/hooks/<name>` existing for an already-wired command (classify, don't invent). Do NOT alter the FFHC API or the verdict/exit contract.
  - `.claude/settings.json.example` (H1): Stop chain → `[run-lint-on-stop.sh, run-typecheck-on-stop.sh, quality-check-apps.js, stop.py]`; drop `run-typecheck-apps.js`; remove the "deprecated" comments on the `*-on-stop.sh` hooks; keep `stop.py` (a `hooks/handlers/` command) discoverable by the merge's upstream-discovery.
- **T2 (A + H2) — full re-vendor 0.25.9 + flag-gate.**
  - Byte-copy 0.25.9's CLI-owned assets over Flow's vendored copies: 20 provider skills (+ `references/`) → `.claude/skills/` + `.agents/skills/`; 4 hooks → `.claude/hooks/`; 2 app-agents → `.claude/agents/` + `.codex/agents/`. (These are CLI-OWNED first-party assets — NOT canonical Flow clean-room; do not assert the Flow attestation over them; `docs/source-map.md` documents this boundary.)
  - Run `bash hooks/local/stamp-cli-provenance.sh` → fresh `audit/cli-vendor-manifest.json` (sha + count; `source_cli_version` stays the `unknown` sentinel unless the script derives it).
  - `agent-surface-ownership.json` (H2): add `app-api-contract-testing` to `known_names` (both `.claude` + `.agents` cli-provider-skill entries) AND to `flag_gated_skills` → `{"app-api-contract-testing": ["cross-app-api-calls-analysis"]}` (use the verified flag name). Bump the CLI-skill count 19→20 wherever the AGENTS/overlay catalog asserts it.
- **T3 (C) — docs.** `docs/compatibility.md` (19→20; the 0.25.9 wired-hook set; `38=19x2`→`40=20x2`; version ref), `docs/source-map.md` (CLI-provider count), `README.md` ("19 ... provider skills" → 20 + hook/count sections), `docs/fusebase-cli-edition.md` (CLI Stop-hook description), `audit/README.md` (count), `stamp-cli-provenance.sh` comments. Historical release notes stay historical.
- **T4 (D) — tests.** NEW `hooks/tests/test-cli-0259-compat.sh` (or extend an existing): (a) conflict reporter on a 0.25.9-shaped fixture (3 CLI hooks + Flow stop.py, no run-typecheck-apps.js) → NO `SHARED_MERGE_DRIFT`/no false "not preserved" (**RED-then-GREEN** vs the pre-fix code), AND a fixture where a previously-wired CLI hook was dropped → STILL flagged; (b) `settings-json-merge.py` on a 0.25.9 settings → does NOT add run-typecheck-apps.js, preserves the 3 hooks + enabledMcpjsonServers, appends stop.py once, idempotent; (c) `app-api-contract-testing` flag-OFF + absent → benign INFO not `CLI_LAYER_DRIFT`; (d) re-vendor freshness — vendored sha == manifest. **UPDATE `hooks/tests/test-cli-flow-recovery.sh`** — replace the old assertions (run-typecheck-apps.js preservation / `*-on-stop.sh` deprecated-non-reinjected) with the 0.25.9 model. Wire new tests into run-tests.sh. **No-regression:** the 26 health-check timeout tests + non-FuseBase/0-present benign behavior + FFHC API unchanged. Genuine RED-then-GREEN, loud asserts, no false-green. Re-mirror only if a Flow skill/agent changed (none expected — CLI assets are vendored, not Flow-mirrored).

## FR-07 / hard rules
No diff to FLOW_RULES FR rows / the 3 deploy-policy rule semantics / ratchet-governance.yml. The health-check stays READ-ONLY and never calls `fusebase`. Don't change the FFHC `ffhc_*` API or the verdict/exit contract. Re-vendored CLI assets stay CLI-owned (clean-room boundary). Do NOT bump VERSION/push/deploy.

## Per-commit pre-attestation
```
☐ preflight 0/0  ☐ worker-undisturbed unchanged  ☐ one task scope  ☐ no TODO/FIXME/WIP
☐ FR-22 comments  ☐ FR-25 <ceiling  ☐ health-check read-only/no-fusebase-calls  ☐ FFHC API + verdict/exit contract intact
☐ FLOW_RULES FR rows / 3 deploy-policy semantics / ratchet UNCHANGED  ☐ commit cites the task
```

## Gate (stop, report, HALT)
preflight 0/0 · `bash -n` the two shells + `python -m py_compile settings-json-merge.py` · run-tests PASS incl. new + updated tests + the 26 health-check timeout tests (no-regression) · check-module-size --all exit 0 · mirror 0 drift (Flow skills/agents) · `cli-vendor-manifest.json` fresh (no CLI_SNAPSHOT_STALE on the refreshed snapshot) · FR-07 clean. Emit the FR-22 marker. Produce the gate report; HALT. A FuseBase adversarial review runs after the gate.

## Return
Gate report: per-task SHAs (T1–T4), AC evidence (B diff-framed no-false-positive RED-then-GREEN + still-flags-dropped; merge no-double-typecheck/preserve/idempotent; H1 example updated; H2 flag-gated benign; A re-vendor freshness + manifest sha match), the 26-timeout no-regression, gate numbers, FR-07 + read-only + FFHC-API confirmation, and the clean-room boundary note (CLI assets vendored, not under Flow attestation).
