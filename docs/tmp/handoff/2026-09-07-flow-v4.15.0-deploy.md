# Deploy handoff - Fusebase Flow v4.15.0

**Status:** APPROVED FOR DEPLOY
**Gate source:** `2a886efabf093897c7827337b05980e414ae6348`
**Implementation source:** `e99d61bff2b189f8801d538f3b6c8e20c04232b0`
**Review:** GPT-6 Astra Medium final audit APPROVED, zero blockers
**Operator authorization:** production deployment authorized in chat; operator directed `proceed to finalize`
**Security:** N/A - no auth, permissions, secrets, migration, customer-message, or production-data surface
**UI/audience:** N/A - repository framework release
**Rollback surface:** code/release publication

## Preconditions

| Check | Required |
|---|---|
| Tracked worktree | clean; preserved unrelated untracked files remain unstaged |
| Worker-undisturbed | policy list empty; final re-check required |
| Version | four carriers equal `4.15.0` |
| Local integrity | hook `212/212`; managed `375/375` |
| Consumer proof | `HEALTHY`, rc0; evidence at `C:/Users/Pavel/AppData/Local/Temp/final-cli-recovery-1788792842096/` |
| Existing tag | `v4.15.0` absent locally and remotely |

## Commands

1. `git push origin main`
2. Verify remote `main` resolves to the reviewed SHA and `v4.15.0` remains absent. Ordinary pushes do not trigger maintainer CI.
3. `git tag -a v4.15.0 -m "Fusebase Flow v4.15.0" 2a886efabf093897c7827337b05980e414ae6348`
4. `git push origin v4.15.0`
5. Wait for the exact-tag-SHA release workflow Linux, Windows/MSYS, aggregate gate, tag-target, and publish jobs. Do not create a GitHub Release manually.
6. `bash hooks/local/print-release-fingerprints.sh v4.15.0`; append its generated row to `docs/release-fingerprints.md`, commit separately on `main`, then `git push origin main`. Never amend or move the tag.

## Probes

| ID | Probe | Pass criterion |
|---|---|---|
| P1 | remote `main` | resolves to `2a886efabf093897c7827337b05980e414ae6348` before fingerprint commit |
| P2 | tag target | local and remote `v4.15.0^{}` resolve to P1 SHA |
| P3 | tagged release workflow | Linux, Windows/MSYS, aggregate verification, tag-target, and publish succeed at P1 SHA |
| P4 | GitHub Release | published release tag is `v4.15.0`; target is P1 SHA; release note is `docs/release-notes/v4.15.0.md` |
| P5 | fingerprint | generated `v4.15.0` row committed once on post-tag `main`; tag target unchanged |

## Smoke

| ID | Outcome | Ground truth | Adversarial check |
|---|---|---|---|
| S1 | consumers can identify the immutable release | GitHub release API and remote annotated tag | release/tag SHA differs from P1 SHA |
| S2 | release assets were created only after verification | tagged workflow job graph and conclusions | publish succeeds while a required verification job is red/skipped |

## Rollback / failure handling

- Before tag push: fix or revert on `main`, push normally, and re-run verification.
- After tag push: the tag is immutable. Record its fingerprint even on red CI; fix on `main` and cut a new version/tag. Never force-push, delete, amend, or move `v4.15.0`.
- Preserve consumer originals, CLI/provider assets, and the unrelated untracked backup/evidence paths.

## Completion record

Fill after publication: main push SHA, main CI run, tag object/peeled SHA, tagged CI run/jobs, release URL, fingerprint commit SHA, probe/smoke results, elapsed time.
