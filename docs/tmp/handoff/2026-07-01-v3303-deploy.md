# Deploy handoff — FuseBase Flow v3.30.3 (Windows/MSYS hardening: WS1-WS9 + review corrections)

## Role bootstrap
AI Developer in **DEPLOY PHASE** under FuseBase Flow v3.30.2→v3.30.3. Self-attest FR-01..FR-27 + IM.1..IM.18 + the Deploy-phase discipline (DP.1..DP.12). **Synchronous; bound long runs (host saturated); no runaways.** This is a real PRODUCTION release (VERSION bump + push + tag + GitHub release + FR-14 docs closeout).

## Deploy authorization (DP.1 / DP.6)
- **DP.1 approval artifact:** the operator gave STANDING full authorization for this autonomous v3.30.3/v3.30.4 roadmap run — "deploy to production if needed. You have my full authorization" (and no real data/users on production; migrations authorized). The PO authors the DP.1 deploy-approval artifact `state/approvals/deploy-v3.30.3-<date>.json` (gitignored) before you run; confirm it exists.
- **DP.6 magic phrase:** the operator's standing authorization for this roadmap is the DP.6 equivalent for this run (recorded in the PO's dispatch). Proceed on that basis; do NOT wait for a fresh interactive phrase (operator is away).
- Two independent adversarial reviews (Codex + FuseBase workflow) of the full corrected diff returned SHIP / SHIP-WITH-FIXES-now-fixed; all findings folded (T10-T18).

## Release candidate
- HEAD to release: **`b396477`** (T18) — v3.30.3 candidate; 13 commits `d6d2985..b396477` on v3.30.2 `12a543f`. G6/T18 gate GREEN; final Codex T18-delta review SHIP. Verify HEAD == `b396477` before starting.
- Branch: `main`. Remote: `origin`. Current published: `12a543f` (v3.30.2).

## Pre-deploy verification (do FIRST — abort on any failure)
1. `bash hooks/local/preflight.sh` → expect 0 errors / 0 warnings.
2. **SINGLE** `bash hooks/local/mirror-skills.sh --check` → 0 drift (32 skills, 86 mirror files). Do NOT run it more than once (concurrent mirror runs corrupted the manifest in the v3.30.2 deploy).
3. Confirm `audit/skill-mirror-manifest.txt`: raw line count == unique == **86**, **0 duplicates**. Abort if not.
4. `git status` clean except the intended untracked docs (roadmap, problem-catalog, handoffs).
5. **Re-read `VERSION` immediately before writing any version string** — it must read `3.30.2` now.

## Deploy steps (one release commit + one FR-14 docs commit)
1. **Bump VERSION** `3.30.2` → `3.30.3`.
2. **Propagate version strings**: run `bash hooks/local/sync-version-strings.sh` ONCE (it writes README/plugin manifest/etc.). It bounds itself — let it finish; do NOT run concurrent mirror/sync. After it completes, re-verify manifest raw==unique==86 + 0 dups and a SINGLE `mirror-skills.sh --check` → 0 drift. If the manifest drifted, regenerate cleanly (`mirror-skills.sh` once) and re-verify before committing.
3. **Release commit**: stage VERSION + the synced public files (explicit paths, NOT `git add -A`); commit `release(flow): v3.30.3 — Windows/MSYS hardening (WS1-WS9) + adversarial-review corrections`. Commit through the wired pre-commit (NO --no-verify); if it trips FR-07 on a protected path, mint the sanctioned single-use bootstrap approval FIRST.
4. **Push** `main` to `origin`.
5. **Tag** `v3.30.3` (annotated) and push the tag.
6. **GitHub release** `gh release create v3.30.3` with notes summarizing: WS1 secret-scan runtime tokens + single-use digest-bound protected-path exception + safe git-hook (re)install; WS2-core/WS3 MSYS bounded-run strict winpid scoping + harness reap + **fixture-phase stdin fix (T17/T18)**; WS4 health-check verdict robustness + MSYS timeout defaults; WS6 backward-compatible dual-marker migration; WS7 problem catalog; WS8 zero-trust liveness FR-27 extension; WS9 command naming; + the adversarial-review corrections (glob-bypass close, unique hook marker, trap re-verify, test fidelity). Note v3.30.4 will add WS2-hard (Job Object) + WS5 (upgrade busy-loop).
7. **Capture the deploy hash** (release commit SHA) for the closeout.

## Post-deploy — FR-14 SINGLE docs commit
One commit `docs(post-deploy): v3.30.3 WS1-WS9 DONE + roadmap/problem-catalog committed`:
- Flip `docs/specs/windows-msys-hardening/roadmap.md` workstreams WS1-WS9 (+ the folded corrections) to DONE with the release SHA + tag.
- COMMIT the previously-untracked `docs/specs/windows-msys-hardening/` (roadmap — the execution source of truth) and `docs/problem-catalog/` (WS7).
- Mark the relevant backlog/spec tickets DONE per the repo's convention.
- (The `docs/tmp/handoff/*.md` implement/deploy handoffs may be committed as the audit trail or left untracked per repo convention — follow existing precedent.)

## Smoke / probes (post-release)
- `bash hooks/local/fusebase-flow-health-check.sh` → HEALTHY (or a known-benign verdict on this saturated host; capture output).
- On a quiet MSYS box (or note as consumer-verify): `bash hooks/tests/run-tests.sh` full-suite — the fixture phase should now pass under the bounded wrapper (T17/T18 deliverable). Capture the Total.
- Confirm `git tag --points-at HEAD` shows v3.30.3 and the GitHub release is live.

## Hard rules
Do NOT `--no-verify`. Do NOT force-push. One release commit + one FR-14 docs commit (no scattered commits). Re-read VERSION before writing it anywhere. SINGLE mirror check. Verify manifest 86/86/0-dups before AND after the version-string sync. If anything is ambiguous or a check fails, STOP and report — do not improvise a workaround on a production release.

## Return
Deploy report: release commit SHA + tag + GitHub release URL, VERSION now 3.30.3, manifest 86/86/0-dups confirmed, mirror 0 drift, smoke/probe results, the FR-14 docs commit SHA, and the DP.1 artifact used. Note any consumer-verify items (full run-tests on a quiet box).
