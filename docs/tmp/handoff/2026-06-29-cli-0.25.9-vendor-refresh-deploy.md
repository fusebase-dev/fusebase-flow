# Deploy handoff — cli-0.25.9-vendor-refresh → v3.30.0 (MINOR)

## Role bootstrap
You are the **Deploy phase** (AI Developer) under FuseBase Flow v3.29.0 → shipping **v3.30.0**. Self-attest FR-01..FR-27 + DP section. **DP.1 approval artifact:** operator authorized the full deploy with the magic phrase **`APPROVE-DEPLOY-NOW`** (2026-06-29) — this is DP.6 confirm. **Run SYNCHRONOUSLY — no background monitors (FR-27: bound/poll any long check; never leave a run bare).** Re-read VERSION right before writing any version number into a public file. "FuseBase" is always two capitals.

## What ships
**FuseBase CLI 0.25.9 vendor refresh + de-staled CLI-Stop-hook model.** Flow v3.29.0 ran with CLI 0.25.9 but its vendored snapshot + hardcoded Stop-hook model were stale → a proven false `SHARED_MERGE_DRIFT` + a redundant double-typecheck on restore. This release: (T1) preserve-only merge + **diff-framed** health-check (no false positive; older-CLI projects keep their `run-typecheck-apps.js`; never re-injects it); (T2) full re-vendor to 0.25.9 (20 provider skills incl. new `app-api-contract-testing`, 4 hooks, 2 agents; manifest re-stamped) + flag-gate the new skill (`cross-app-api-calls-analysis`); (T3) docs to 0.25.9 reality (19→20 skills, hook set); (T4) new `test-cli-0259-compat.sh` (12) + recovery test updated to the 0.25.9 model. **No new FR rule (FR-01..FR-27 unchanged), no new Flow skill (32 unchanged) — the 20 is the CLI-provider count, separate from the 32 Flow skills.** 4 local commits on `origin/main` `d4eabb9`, HEAD `d0b25e4`. **Both reviews SHIP:** Codex design RESCOPE folded; FuseBase adversarial impl review **SHIP** against the real 0.25.9 CLI (merge no-double-typecheck + idempotent; health-check HEALTHY; 62/62 byte-match; 1 tracked MED + 2 LOW, none blocking).

## Step 1 — version bump
- Re-read `VERSION` (expect 3.29.0). Set `VERSION` + `.claude-plugin/plugin.json` `3.29.0` → **3.30.0** (equal).
- `bash hooks/local/sync-version-strings.sh` — verify adapters incl. **GEMINI.md** = v3.30.0; **FR-01..FR-27 + 32 Flow skills unchanged**; under-reach guard passes; **the v3.29.0 AGENTS.md command-equivalents table stays byte-unchanged** (outside swept region); **the T3 doc count changes (19→20 CLI-provider, hook set) are NOT clobbered** by the sweep (they're outside the Flow-skill-count enforcement); no consumer doc touched.

## Step 2 — README
- Badge (line ~9) `3.29.0` → `3.30.0` (manual). Confirm the CLI-provider skill count + hook-set sections read `20` / the 0.25.9 set (added at implement T3).

## Step 3 — release notes + CHANGELOG
New `docs/release-notes/v3.30.0.md` + `CHANGELOG.md [3.30.0]` (date 2026-06-29, deploy hash after release commit): **FuseBase CLI 0.25.9 vendor refresh + de-staled CLI-Stop-hook model.** Cover: the stale-model problem (false `SHARED_MERGE_DRIFT` for `run-typecheck-apps.js` + redundant double-typecheck); the fix (preserve-only merge + diff-framed, version-agnostic health-check; never re-injects `run-typecheck-apps.js`; older-CLI projects keep theirs); full re-vendor to 0.25.9 (20 provider skills incl. new `app-api-contract-testing` flag-gated behind `cross-app-api-calls-analysis`, 4 hooks, 2 agents; manifest re-stamped, 62/62 byte-match to the real CLI); `.claude/settings.json.example` aligned to 0.25.9's wired Stop set; docs 19→20; new compat test suite (12) + recovery test updated. **FR-07-clean / additive:** no FR rule rows, no deploy-policy rule semantics, no `ratchet-governance.yml` touched; CLI-owned vendored assets stay outside the Flow clean-room attestation. Grounded + dual-reviewed (Codex design + FuseBase impl, both SHIP) against the real 0.25.9 CLI. Note the tracked follow-ups (backlog, Step 7).

## Step 4 — final gate
preflight 0/0 · `bash -n hooks/local/check-cli-flow-conflicts.sh hooks/local/stamp-cli-provenance.sh` + `python -m py_compile hooks/local/fusebase-flow-overlays/settings-json-merge.py` · run-tests **164/164** PASS · check-module-size --all exit 0 · mirror 0 drift · plugin==VERSION==3.30.0 · the 5 FR-07 surfaces UNCHANGED (FLOW_RULES FR rows, approval-policy, protected-paths, command-policy, ratchet-governance) · `cli-vendor-manifest.json` fresh (no `CLI_SNAPSHOT_STALE`) · git clean after the release commit.

## Step 5 — release
1. `git push origin main`.
2. `git tag -a v3.30.0 -m "FuseBase Flow v3.30.0 — FuseBase CLI 0.25.9 vendor refresh + de-staled CLI-Stop-hook model"`; `git push origin v3.30.0`.
3. `gh release create v3.30.0 --title "v3.30.0 — FuseBase CLI 0.25.9 vendor refresh" --notes-file docs/release-notes/v3.30.0.md --latest`.
4. Capture deploy hash.

## Step 6 — probes + smoke (capture evidence)
- mirror byte-identical; sync --dry-run framework-only; GEMINI.md = v3.30.0; README badge = 3.30.0; FR-01..FR-27 + 32 Flow skills; `git grep -ni headroom` in code/skills/hooks/manifest = 0.
- **Ticket smoke (build a fixture from the REAL extracted 0.25.9 CLI** — re-extract `"/c/Users/abcpa/OneDrive/Downloads/apps-cli-main (6).zip"` if needed**):**
  - `settings-json-merge.py` on the real CLI settings → appends `stop.py` once, preserves the 3 CLI hooks + `enabledMcpjsonServers`, adds **0** `run-typecheck-apps.js`; re-run byte-identical (idempotent).
  - `check-cli-flow-conflicts.sh` on the merged fixture → **HEALTHY**, `shared_merge_drift:0`, `cli_snapshot_stale:0`.
  - `app-api-contract-testing` flag-OFF+absent → benign (HEALTHY); 20 provider skills present.
  - `.claude/settings.json.example` Stop chain = `[run-lint-on-stop.sh, run-typecheck-on-stop.sh, quality-check-apps.js, stop.py]`, no `run-typecheck-apps.js`.
  - Re-stamp manifest in a copy → 0 diff. Clean up the fixture.

## Step 7 — single FR-14 docs commit
- Flip `docs/specs/cli-0.25.9-vendor-refresh/spec.md` → **DONE** + deploy hash + tag + release URL.
- File backlog follow-ups (from the FuseBase review, both tracked, neither blocking):
  - `docs/backlog/healthcheck-diff-source-hardening/README.md` — the MED: the diff-framed reporter's evidence (`.claude/settings.json.pre-flow-merge`) is ephemeral; when absent a genuinely-dropped CLI hook isn't flagged (deliberate no-false-positive trade-off; merge is preserve-only so Flow never drops a hook). Real fix = backup-persistence (NOT the "on-disk-but-unwired" fallback — that would re-introduce the run-typecheck-apps.js false positive). 
  - `docs/backlog/cli-custom-at-risk-overflag/README.md` — the LOW: `CLI_CUSTOM_AT_RISK` advisory over-flags CLI-shipped CUSTOM blocks (e.g. `app-dev-practices`); cosmetic, advisory-only.
  - Add `docs/backlog/index.md` rows if that index exists.
- Push. Output the deploy report.

## Hard rules
FR-07: FR-01..FR-27 rows + 3 deploy-policy semantics + ratchet UNCHANGED (version attestation lines allowed). Re-vendored CLI assets stay CLI-owned (clean-room boundary; not under Flow attestation). Keep `internal/` + repo-polish + `.claude/settings.local.json` + the `*-implement.md`/`*-deploy.md` handoffs untracked. If any gate/probe fails, STOP and report.

## Rollback
`git revert <release range>` — additive (re-vendored CLI assets + a health-check/merge behavior fix that only REMOVES a false positive + a redundant hook + docs/tests); no FR rule/skill added; older-CLI projects unaffected (merge still preserves their hooks). Re-push; re-mirror.

## Return
Deploy report: version, deploy hash, tag, release URL, GEMINI + README badge = v3.30.0, FR-01..FR-27/32-Flow-skills confirmation, the ticket smoke (real-CLI merge no-double-typecheck + idempotent; health-check HEALTHY; example wired; 20 skills; manifest 0-diff; headroom=0), FR-07 confirmation, and the FR-14 docs commit SHA (spec DONE + 2 backlog items filed).
