NO-SHIP. The bound-verifier mechanics are mostly correct, but M14 permits a pre-authorization layer downgrade; that is evidence the contract itself is wrong, not another finite coding error.

## Verdict table

| Area | Verdict | Finding |
|---|---|---|
| M13 binding | **PARTIAL** | The set is fixed at [bootstrap-upgrade.sh:249](<C:/Users/Pavel/projects/fusebase-flow-publish/fusebase-flow-FuseBase CLI edition/hooks/local/bootstrap-upgrade.sh:249>) immediately before `ff_repair_managed`, and is never re-derived. However, `.git/info/exclude` may be written at lines 413–414 before binding. Thus it is before any **repair-target/managed** write, but not literally “before any write” as M13/M14 state. |
| Bound-layer verification | **PASS** | Manifest absence fails; a wrapper present at authorization must remain present; trusted verifier availability is required; success requires `rc == 0 && verdict == MATCH` at lines 283–317. Exact JSON parsing rejects prefixes, substrings, trailing output, malformed JSON, and duplicate keys. |
| Three-verifier agreement | **PASS** | `ff_boot_repair_verify`, `ff_boot_verify`, and `_ff_mms_verify` all require rc 0 plus parsed exact `MATCH`. |
| Empty bound set | **CODE PASS / TEST GAP** | Lines 264–267 refuse it before `ff_repair_managed`, but there is no test for the branch or for the literal no-repository-write promise. |
| M14 membership safety | **BLOCKER** | “Neither artifact present means not carried” is indistinguishable from “both artifacts were removed before authorization.” This enables a downgrade without any race. |
| Partial install | **ASYMMETRIC** | Manifest/no-wrapper AC3 works safely because the proven source module verifies the manifest directly. Wrapper/no-manifest does not “verify through whichever half it has”; it fails at line 285. M14’s symmetric wording is inaccurate. |
| M15 | **ACCEPT WITH RESIDUAL** | 900s is reasonable headroom for a measured 542s completion and does not false-green assertions/timeouts. It does accept a real scalability regression and delays genuine-hang detection by 420s. |
| Release notes | **PARTIAL** | Plain-source disclosure and git/worktree re-stamp claim are accurate. The M13 behavior bullet overclaims missing-wrapper behavior and omits the pre-authorization downgrade. |

### M14 downgrade

A deterministic attack needs no race:

1. Before authorization, remove both `audit/managed-content-manifest.json` and `hooks/local/verify-managed-content-manifest.sh`.
2. Leave a managed-only drift such as `workflows/wf.md`, plus a repairable hook drift.
3. The managed layer is not bound because both artifacts are absent.
4. Include the missing wrapper among the hook-manifest-reported repair paths.
5. Repair restores the wrapper and hook path; the hook layer returns `MATCH`.
6. The managed layer remains unverified, its manifest remains absent, and the unrelated workflow drift remains.
7. Repair exits 0.

A concurrent process can arrange the same state during the unlocked `-f` probes. More fundamentally, without a durable expected-layer record outside both artifacts, “never installed” and “both removed” cannot be distinguished.

That makes M14 safe only as a compatibility assumption in a non-adversarial consumer tree. It is not safe as the basis for a whole-tree repair claim.

## Tests and regressions

The five new rows are not four genuine RED discriminators:

| Case | At `9c161b1` | Label |
|---|---:|---|
| Parsed `MATCH`, verifier rc 1 | FAILS new test | Genuine RED |
| Manifest vanishes after authorization | FAILS new test | Genuine RED |
| Manifest already absent, wrapper anchors layer | FAILS new test | Genuine RED |
| Wrapper vanishes | Already returned nonzero; only attribution differs | Correctly labelled coverage repair |
| Clean two-layer repair | Passes both baselines | Control, not RED |

The implementation commit message accurately claims **three** genuine REDs, one coverage repair, and one both-green control. Any claim of four genuine REDs is incorrect.

Reverting only `bootstrap-upgrade.sh` under HEAD’s tests is a valid focused mutation test because the M13 production behavior changed only there. It proves the three discriminators depend on that delta. It proves less than transplanting the tests onto an exact `9c161b1` checkout, and a full-harness run would also encounter deliberately stale manifests after such a one-file revert.

Regression posture:

- B1–B7, R1–R5, K10, source-boundary behavior, cleanup exact authority, and `path_policy` fail-closed logic remain intact. Their implementation files are unchanged where applicable, and the full reported gates are green.
- B8’s rc/exact-verdict defects are closed, but the broader repair guarantee remains open through M14.
- The recorded Windows gate confirms both the existing manifest/no-wrapper AC3 case and clean two-layer control pass at [hook-test-results.md:733](<C:/Users/Pavel/projects/fusebase-flow-publish/fusebase-flow-FuseBase CLI edition/state/audit/hook-test-results.md:733>).
- Both current manifests independently verify `MATCH`: 140/140 and 286/286.
- No cleanup-authority, secret, permission, FR-07, module-size, or comment-policy regression found.
- Git Bash could not start in this read-only Windows sandbox (`CreateFileMapping`, error 5), so I relied on the recorded Windows artifact, supplied Linux result, and static trace rather than rerunning Bash.

The plain-source disclosure at [v4.7.0.md:130](<C:/Users/Pavel/projects/fusebase-flow-publish/fusebase-flow-FuseBase CLI edition/docs/release-notes/v4.7.0.md:130>) is accurate and sufficient: one authority proves internal consistency and snapshot-to-use transit integrity, not upstream authenticity. The git worktree re-stamp claim remains intact relative to committed objects. Line 142’s statement about interpreter hardening is narrowly true; the broader behavior claim at line 105 is not.

## Ship verdict

**NO-SHIP for v4.7.0.**

| Must-fix | Classification |
|---|---|
| Reopen M14: add an independent expected-layer record/trust anchor, or explicitly narrow repair success to currently carried coverage and disclose the pre-authorization downgrade. | **(b) Evidence the contract itself is wrong — operator decision required** |
| Bind before `.git/info/exclude` or any other repository write, unless M13 is explicitly narrowed to “before any managed/repair write.” | **(a) Finite implementation/wording defect** |
| Add pre-authorization dual-removal, empty-set/no-write, and wrapper-only asymmetry cases; correct the “four RED” claim and release-note overstatement. | **(a) Finite tests/docs defects** |

M15 is not a ship blocker, but the 542s phase is a real performance residual: repeated whole-project/skill-tree copies should be optimized, and 900s increases hang-detection latency.

---
Phase: Verify  
Ticket: msys-v3307-hardening  
Next: Return M14 to the operator for contract disposition; implementation follows only after that decision.