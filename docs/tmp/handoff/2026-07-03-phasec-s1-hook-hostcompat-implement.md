# Implement handoff — Phase C Slice 1: hook host-compatibility (make live Claude Code enforcement actually fire)

## Role bootstrap
AI Developer (Opus 4.8) under FuseBase Flow v3.30.6. Self-attest FR-01..FR-27 + IM.1..IM.18. Load-bearing: FR-05, FR-07 (EDITS protected handlers — note), FR-10 (reproduce-before-fix — mandatory here), FR-12, FR-22. **Synchronous; bound long runs (host saturated; export FF_CLI_RECOVERY_TIMEOUT=600 FF_PHASE_TIMEOUT=900); no runaways; STOP at gate; do NOT bump VERSION/push/tag/deploy.** ONE coherent commit stacked on HEAD 676f4b1 (v3.30.6). This is a CORRECTNESS fix from the Phase C audit: the Flow enforcement hooks are INERT in real Claude Code sessions because they read Flow-schema keys the runtime never sends. This fix makes inert gates FIRE — it does NOT weaken any gate.

## PROTECTED-PATH NOTE (FR-07)
Edits `hooks/handlers/user_prompt_submit.py` AND `hooks/handlers/stop.py` (both `fusebase_flow_internals` protected). Fixtures under `hooks/tests/fixtures/**` + `hooks/tests/*.sh` + docs are NOT protected. REFRESH the wired hook first (`bash hooks/local/install-git-hooks.sh`), mint ONE sanctioned single-use bootstrap approval whose tree_digest binds the ENTIRE staged changeset (both protected handlers together), commit, `--consume`, confirm `.git/hooks/pre-commit` == source. NEVER `--no-verify`.

## VERIFIED problem (confirm with FR-10 reproduce-before-fix)
- `user_prompt_submit.py:84`: `user_prompt = event.get("user_prompt") or ""` — Claude Code's native UserPromptSubmit sends the prompt under key `prompt`, NOT `user_prompt`. REPRODUCE: pipe `{"hook_event_name":"UserPromptSubmit","prompt":"... ghp_<40charPAT> ..."}` into the handler → currently returns allow/clean; the same text under `user_prompt` returns warn (FR-12). So the secret warn, bypass/impl-without-spec detection, and /product-owner reminder are inert live.
- `stop.py:145`: CLAIM_PATTERNS are matched against `agent_message` ONLY; Claude Code's Stop event carries no `agent_message` (it carries `transcript_path`). REPRODUCE: a native Stop event (no agent_message, transcript_path → JSONL whose last assistant message says "Implementation complete. Ready to deploy.") → currently allow/exit0; the same claim via `agent_message` → deny/exit2. So the FR-04/05/14 done/deploy deny gate + FR-22 recommended-warn are inert live. (stop.py DOES read transcript_path at :125 but only for the PO-activation warn + `_signals_from_transcript`, NOT for claim DETECTION at :144-147.)
- No normalization shim exists (settings.json.example invokes handlers directly; README/schema falsely imply one).

## Scope — ONE commit
1. **user_prompt_submit.py:** dual-key read — `user_prompt = event.get("user_prompt") or event.get("prompt") or ""`. Keep everything else. (Backward-compatible: existing fixtures using `user_prompt` still work.)
2. **stop.py claim detection:** when `agent_message` is empty/absent, derive the FINAL assistant message from the TAIL of `transcript_path` (parse the JSONL; take the last entry with role=="assistant" and extract its text content — handle Claude Code's transcript JSONL shape). Run CLAIM_PATTERNS against `(agent_message or final_assistant_text)`. Do NOT run CLAIM_PATTERNS against the WHOLE transcript_text (that would over-trigger on any historical claim phrase) — use ONLY the final assistant message. Keep the existing agent_message path working (backward-compatible). Also relax `_PO_INVOCATION_RE` so a real transcript JSONL line (`"content":"/product-owner"` or `<command-name>/product-owner`) matches (the finding notes the current `(?:^|\s)/product-owner` no-MULTILINE misses it).
3. **Surface the results to the model:** for a warn, in addition to stderr, emit `hookSpecificOutput.additionalContext` (JSON stdout) so the warning actually reaches the model (exit-0 JSON stdout `{"decision":"allow"}` does not surface a warning). For a deny (exit 2), print the reason to STDERR before exiting (Stop exit-2 semantics feed stderr, not stdout JSON) — currently the deny path prints nothing. Confirm the additionalContext shape against Claude Code's hook JSON contract (do not break the allow/deny exit-code contract).
4. **Docs correction (non-protected):** update `docs/hook-coverage.md` + `flow-skills/role-discipline/references/rail-mapping.md` + `FLOW_RULES.md` FR-12 enforcement column (L7 — add pre_tool_use/the real enforcing hook) to state the TRUE per-host field coverage (do not over-claim). If FLOW_RULES.md (protected) is edited, it's covered by the same bootstrap approval digest.
5. **Tests (RED→GREEN) — the coverage-gap closer:** add NATIVE-shape fixtures + asserts:
   - a UserPromptSubmit fixture with the native `prompt` key carrying a secret → warn FIRES (RED on 676f4b1: allow; GREEN: warn). Keep the existing `user_prompt` fixture passing.
   - a Stop fixture (native shape: NO agent_message, a transcript_path pointing at a JSONL whose last assistant message makes a done-claim without the gate signals) → deny/exit2 FIRES (RED on 676f4b1: allow; GREEN: deny). Keep the existing agent_message fixtures passing.
   - a fixture proving a done-claim EARLIER in the transcript (not the final message) does NOT trigger (no over-trigger).

## Do NOT
- Do NOT weaken any gate, pattern, or signal requirement (this fix makes inert gates fire; the deny/warn LOGIC is unchanged — only the INPUT source is fixed). Do NOT touch the FR-07/FR-12 pre-commit chain (hooks/git/pre-commit, hooks/shared/**) — S1 is the HANDLERS only. Do NOT bump VERSION/push/tag. Do NOT `--no-verify`.

## Gate (scoped) — stop, report, HALT
Run the fixture/handler phase (FF_ONLY=fixtures) + the po-verifiable-boot + fr22-delivery suites (they exercise stop.py/user_prompt_submit) + any secret-scan fixture phase, all bounded 0-FAIL, incl. the NEW native-shape fixtures RED→GREEN. `py_compile` the 2 handlers; `bash -n` the tests. preflight green; SINGLE mirror --check 0-drift (if docs/skills touched); check-module-size --all exit 0. FR-07 clean (sanctioned approval consumed); `.git/hooks/pre-commit` == source. Emit FR-22 marker; gate report; HALT.

## Return
Gate report: commit SHA; FR-10 reproduce evidence (native-shape RED on 676f4b1 → GREEN); the 3 new fixtures RED→GREEN + existing fixtures still green (backward-compat); confirmation the deny/warn LOGIC is unchanged (only input source fixed, no gate weakened); additionalContext/stderr surfacing added; docs corrected; FR-07 approval used + .git/hooks==source; scoped-gate numbers; module-size/mirror clean. Do NOT push/tag/deploy.
