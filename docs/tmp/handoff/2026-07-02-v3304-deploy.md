# Deploy handoff — FuseBase Flow v3.30.4 (WS2-hard opt-in Job Object fence + WS5 upgrade bounded exit)

## Role bootstrap
AI Developer in **DEPLOY PHASE** under FuseBase Flow v3.30.3→v3.30.4. Self-attest FR-01..FR-27 + IM.1..IM.18 + Deploy-phase discipline (DP.1..DP.12). **Synchronous; bound long runs (host saturated); no runaways.** Production release (VERSION bump + push + tag + GitHub release + FR-14 docs closeout). Work SYNCHRONOUSLY; do NOT spawn background monitors; if a network op fails on a transient, STOP and report exactly where you are.

## Deploy authorization (DP.1 / DP.6)
- Operator STANDING full authorization for the autonomous v3.30.3/v3.30.4 roadmap run ("deploy to production if needed. You have my full authorization"; no real data/users on prod; operator away). DP.1 artifact authored: `state/approvals/production_deploy-v3304-20260702.json` — confirm it exists + is unexpired. DP.6 equivalent = that standing authorization; do NOT wait for a fresh interactive phrase.
- v3.30.4 = WS2-hard + WS5 (final roadmap slice). Three review passes on the corrected diff (Codex full BLOCK→fixed, Workflow BLOCK→fixed, Codex re-review SHIP) — all findings folded (T19/T20 + T21/T22 corrections).

## Release candidate
- HEAD to release: **`f449411`** (T22) — v3.30.4 candidate; commits `161182f`(T19) `8ef28d3`(T20) `3a623b2`(T19-fix) `2016552`(T21) `f449411`(T22) on v3.30.3 `cc6ef06`. Verify HEAD == `f449411` and the final Codex re-review was SHIP before starting.
- Branch `main`, remote `origin`. Current published: `cc6ef06` (v3.30.3, tag v3.30.3).

## Pre-deploy verification (do FIRST — abort on any failure)
1. `bash hooks/local/preflight.sh` → 0 errors / 0 warnings.
2. **SINGLE** `bash hooks/local/mirror-skills.sh --check` → 0 drift (32 skills, 86 mirror files). Do NOT run it more than once.
3. `audit/skill-mirror-manifest.txt`: raw == unique == **86**, 0 duplicates.
4. `git status` clean except intended untracked docs (handoffs).
5. **Re-read `VERSION`** immediately before writing — must read `3.30.3` now.

## Deploy steps (one release commit + one FR-14 docs commit)
1. **Bump VERSION** `3.30.3` → `3.30.4`.
2. **Bump `.claude-plugin/plugin.json`** version `3.30.3` → `3.30.4` — it is NOT written by sync-version-strings.sh (preflight §8 parity check); bump it together with VERSION or preflight fails. (Known release gotcha.)
3. **Propagate version strings**: run `bash hooks/local/sync-version-strings.sh` ONCE; let it finish; then re-verify manifest 86/86/0-dups + a SINGLE `mirror-skills.sh --check` = 0 drift. If drift, regenerate cleanly (mirror-skills.sh once) and re-verify BEFORE committing.
4. **Release commit** (explicit paths, NOT `git add -A`; NO --no-verify): the wired `commit-msg` allowlist does NOT include `release`, so use the `flow` type — subject: `flow(release): v3.30.4 — WS2-hard opt-in Job Object fence + WS5 upgrade bounded exit`. (Known release gotcha.) If pre-commit trips FR-07 on a protected path (e.g. FLOW_RULES.md via sync), mint the sanctioned single-use bootstrap approval FIRST, commit, then `--consume`.
5. **Push** main to origin.
6. **Tag** `v3.30.4` (annotated) + push the tag.
7. **GitHub release** `gh release create v3.30.4` with notes: WS2-hard = an OPT-IN (`FFHC_USE_JOB_OBJECT=1`, default OFF) Windows Job Object OUTER FENCE around the bounded run — atomic strictly-scoped kill of the assigned child tree; the mechanism is proven on MSYS+PowerShell, the Cummings-class ac3d→rc137 reliability + the Job-Object-vs-`timeout -k` kill discriminator are CONSUMER-GATED (best-effort launch→assign race documented). WS5 = upgrade engine Windows-safe bounded exit: `prune_pre_backups` busy-loop ROOT FIX (per-stem full-tree find → single-pass O(M)), critical-vs-optional bounding (optional step failure/timeout warns+continues, critical fails with a recovery hint), timestamp-safe prune glob. Plus the adversarial-review corrections (fence opt-in hang fix, `set -e` optional-step abort fix tested under `set -e`). Note default behavior is byte-unchanged from v3.30.3 (fence is opt-in).
8. **Capture** the release commit SHA.

## Post-deploy — FR-14 SINGLE docs commit
One commit `docs(post-deploy): v3.30.4 WS2-hard + WS5 DONE`:
- Flip `docs/specs/windows-msys-hardening/roadmap.md` WS2-hard + WS5 to DONE with the release SHA + tag; record the PROVEN-here vs CONSUMER-GATED split in the closeout.
- Add any new problem-catalog entry if warranted (e.g. the set -e-under-sourced-lib class + the tests-ran-without-set-e blind spot — a good guardrail lesson).
- Mark the roadmap COMPLETE (all 9 workstreams shipped across v3.30.3 + v3.30.4).

## Smoke / probes
- `bash hooks/local/fusebase-flow-health-check.sh` → capture verdict (PARTIAL_UNVERIFIED is known-benign on this saturated host — WS4 working as designed).
- Confirm `git tag --points-at HEAD` shows v3.30.4 + `gh release view v3.30.4` isDraft=false.
- Consumer-verify (operator distributes): full `run-tests.sh` on a quiet MINGW64 box; opt-in `FFHC_USE_JOB_OBJECT=1` validation on a real Cummings-class host; full `upgrade.sh --auto-yes` end-to-end.

## Hard rules
NO --no-verify. NO force-push. One release commit + one FR-14 docs commit. Re-read VERSION before writing. Bump plugin.json with VERSION. SINGLE mirror check. Verify manifest 86/86/0-dups before AND after the version-string sync. If any check fails or anything is ambiguous, STOP and report.

## Return
Deploy report: release commit SHA + tag + GitHub release URL, VERSION 3.30.4, plugin.json 3.30.4, manifest 86/86/0-dups, mirror 0 drift, smoke/probe results, FR-14 docs commit SHA, DP.1 artifact used, consumer-verify items.
