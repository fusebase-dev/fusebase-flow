# Deploy handoff — FuseBase Flow v3.30.6 (gate wall-time optimization)

## Role bootstrap
AI Developer in **DEPLOY PHASE** under FuseBase Flow v3.30.5→v3.30.6. Self-attest FR-01..FR-27 + IM.1..IM.18 + Deploy discipline DP.1..DP.12. Production release. Work SYNCHRONOUSLY; do NOT spawn background monitors; if a network op fails on a transient, STOP and report exactly where you are.

## Deploy authorization (DP.1 / DP.6)
- Operator STANDING full authorization for this autonomous roadmap run (operator away; no real data/users on prod). This is Phase B (v3.30.6 gate-loop optimization) of the operator's 3-phase mandate. DP.6 equivalent = that standing authorization.
- Mint the DP.1 artifact for v3.30.6 before release: `state/approvals/production_deploy-v3306-<date>.json` (scope: v3.30.6 gate wall-time optimization; HEAD = the correction commit; unexpired). NOTE: `state/approvals/*` is gitignored local state.

## Release candidate
- HEAD to release: **`5264176`** (v3.30.6 review-corrections) — the v3.30.6 candidate: F2 `6ecec96` (FF_ONLY scoped gates + test-ff-only.sh) + F4 `a2d7971` (preflight batch hashing) + F5 `2374aca` (spawn micro-cuts) + F3 `566085f` (adaptive sub-second poll in the reap loop) + review-corrections `5264176` (F5 empty-TSV-field parse, F3 cap-bound assert ceiling, fixture-leak trap) on v3.30.5 `d43f2f3`. **Verify HEAD == `5264176`.**
- The adversarial deploy review (Codex + a 3-lens Opus panel) confirmed: the optimizations are sound (F3 reap-loop safe — deadline floor preserved, no early reap, timing baseline-equivalent; FF_ONLY fail-closed by construction; F4 byte-identical); the 13 T6 full-gate FAILs are PRE-EXISTING host-saturation artifacts, NOT regressions (3-source empirical confirmation: reap-loop scenarios ac3d/native-descendant reproduce identically on baseline d43f2f3 AND HEAD); the 3 review findings were folded in `5264176`.
- `hooks/git/pre-commit` + `hooks/shared/**` are UNTOUCHED by v3.30.6 (the v3.30.5 fail-closed FR-07/FR-12 chain is fully preserved). Re-verify `.git/hooks/pre-commit` == source.
- Branch `main`, remote `origin`. Current published: `180f4a1` (v3.30.5, tag v3.30.5).

## LOADED-HOST DISCIPLINE
This host is CPU-saturated. Pre-raise `export FF_CLI_RECOVERY_TIMEOUT=600 FF_PHASE_TIMEOUT=900`. Do NOT run the full `run-tests.sh` as a deploy smoke (it's ~40+ min + false-INCONCLUSIVE-prone; v3.30.6 was gated during implement + reviewed). Your deploy smoke = preflight + health-check + tag/release verification. NOTE: v3.30.6 itself makes future gates faster (FF_ONLY scoping + preflight batch), but the deploy uses the light smoke regardless.

## Pre-deploy verification (do FIRST — abort on any failure)
1. `bash hooks/local/preflight.sh` → 0 errors / 0 warnings.
2. **SINGLE** `bash hooks/local/mirror-skills.sh --check` → 0 drift (32 skills, 86 mirror files; NOTE the validation-and-qa skill was edited in F2 for the FF_ONLY note — confirm it's mirrored, manifest 86/86/0-dups).
3. `audit/skill-mirror-manifest.txt`: raw == unique == **86**, 0 dups.
4. `git status` clean except intended untracked docs (NO leaked `zz-ff-only-injected-fail.json`).
5. **Re-read `VERSION`** immediately before writing — must read `3.30.5` now.

## Deploy steps (one release commit + one FR-14 docs commit)
1. **Bump VERSION** `3.30.5` → `3.30.6`.
2. **Bump `.claude-plugin/plugin.json`** `3.30.5` → `3.30.6` (manual, with VERSION — preflight §8 parity).
3. **Propagate**: `bash hooks/local/sync-version-strings.sh` ONCE; re-verify manifest 86/86/0-dups + a SINGLE `mirror-skills.sh --check` = 0 drift.
4. **Release commit** (explicit paths, NOT `git add -A`; NO --no-verify) — the commit-msg allowlist lacks `release`, use: `flow(release): v3.30.6 — gate wall-time optimization (FF_ONLY scoped gates + preflight batch + adaptive reap poll)`. If pre-commit trips FR-07 (VERSION/plugin.json/FLOW_RULES.md via sync are protected), mint the sanctioned single-use bootstrap approval FIRST (tree_digest binding the whole staged release changeset), commit, then `--consume`.
5. **Push** main to origin. 6. **Tag** `v3.30.6` (annotated) + push. 7. **GitHub release** `gh release create v3.30.6` with notes: **Gate wall-time optimization (test/tooling only — no runtime behavior change; coverage + fail-closed preserved).** (F2) `FF_ONLY` opt-in scoped gates in run-tests.sh — run only named suites during the implement loop (unset ⇒ byte-identical full gate; scoped ⇒ fail-closed-by-construction: the scoped summary deliberately fails the strict `N/N PASS` classifier + writes a separate results file + exits 2 on an unknown/empty tag, so a scoped run can never be mistaken for a clean full pass), + a new `test-ff-only.sh` suite. (F4) preflight mirror-hashing batched — one `sha256sum` per root instead of ~270 per-file spawns (~6.7× faster preflight; repairs the default health-check preflight budget; byte-identical drift/missing detection). (F5) spawn micro-cuts in the fixture hot loop (builtin file reads + a metadata pre-pass instead of per-fixture `$(echo|sed)`/python). (F3) adaptive sub-second poll in the MSYS bounded-run reap loop — a spawn-free FIFO nap ladder with EPOCHREALTIME accounting (deadline preserved as a hard FLOOR — never reaps early; falls back verbatim to the prior `sleep 1` on any host without the builtin nap). Verified by an independent adversarial review (Codex + Opus panel): no coverage loss, no fail-closed/security weakening, the security pre-commit chain untouched. 8. **Capture** the release commit SHA.

## Post-deploy — FR-14 SINGLE docs commit
One commit `docs(post-deploy): v3.30.6 gate wall-time optimization`:
- If a problem-catalog entry fits, add one for the **gate-loop wall-time** lesson (the saturated-host gate is ~90% of wall time; FF_ONLY scoped gates are the implement-loop mitigation; the final pre-commit/pre-deploy gate MUST still be a full unscoped run — cite `docs/tmp/handoff/2026-07-03-v3306-fable-optimization-spec.md`). Optional — PO discretion.
- Note v3.30.6 in the roadmap closeout if applicable.

## Smoke / probes (LIGHT)
- `bash hooks/local/fusebase-flow-health-check.sh` → capture verdict (PARTIAL_UNVERIFIED benign on the saturated host — though F4 should reduce the preflight timeout; preflight 0/0 is ground truth).
- `git tag --points-at <release-commit>` shows v3.30.6; `gh release view v3.30.6` isDraft=false.
- Consumer-verify (operator distributes): full `run-tests.sh` on a quiet MINGW64 box (confirm test count ≥ pre-change + the FAIL set is the known host-saturation class); an FF_ONLY scoped run on a quiet box.

## Hard rules
NO --no-verify. NO force-push. One release commit + one FR-14 docs commit. Re-read VERSION before writing. Bump plugin.json with VERSION. SINGLE mirror check. Verify manifest 86/86/0-dups before AND after the sync. `hooks/git/pre-commit` must remain UNTOUCHED. If anything is ambiguous, STOP and report.

## Return
Deploy report: release commit SHA + tag + GitHub release URL, VERSION 3.30.6, plugin.json 3.30.6, manifest 86/86/0-dups, mirror 0 drift, smoke/probe results, FR-14 docs commit SHA, DP.1 artifact used, consumer-verify items.
