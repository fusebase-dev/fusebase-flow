# Maintainer testing

Test behavior at the smallest useful boundary: table/parser or function first, then a tiny filesystem/Git repository, then an integrated scenario only when the changed boundary requires it. Use a full disposable app or real CLI cycle only when an integration boundary changes or compatibility evidence is missing, never as routine feedback for every edit. Destructive recovery scenarios use the smallest isolated temporary repository, verify the mutation root, and leave the consumer tree and maintainer worktree clean. Do not copy the full repository or launch recovery to test a string predicate. Use existing fixtures and runners before creating another harness.

## Required guarantees

| Guarantee | Required coverage |
|---|---|
| Install and distribution | Preflight, manifests, mirror parity, command/provider delivery and actual installer scenarios |
| Ownership and upgrade | Merge/classification, source/path boundaries, user/CLI byte preservation and supported upgrade paths |
| Recovery | Prior intent/opt-out, invalid-input zero writes, interrupted-write partial status, retry convergence and no-op |
| Executable safety | Secret scan, protected paths, approval/trusted-enforcer cases, real Git hook allow/deny/failure paths |
| Process lifecycle | Failure/timeout propagation, owned-child cleanup, zero-result refusal and selector completeness |
| Publication | Parsed workflow graph, both platforms, required-job success, manifests and tag/verified-SHA binding |

`hooks/tests/run-tests.sh` owns membership and `FF_LIST=1` lists it. `FF_RELEASE=1` selects an explicit 40-tag release allowlist; a newly registered phase stays out until its consumer or safety responsibility is reviewed and deliberately added. Do that review in the outcome that adds or changes the phase: an essential consumer or safety contract enters the allowlist, an excluded diagnostic gets a brief reason in the table below, and registration alone leaves the outcome incomplete. `FF_FULL=1` runs every non-opt-in diagnostic, while `FF_ONLY` names affected groups. Neither local mode authorizes publication.

That last sentence is now a control, not prose: preflight §11 (`hooks/local/lib/phase_registry_check.py`) rejects any `FF_TAGS` phase that is not classified in exactly one of the three tables below, any release/exclusions table row that disagrees with `FF_RELEASE_TAGS`/`FF_OPTIN_TAGS` in either direction, any row naming a tag that no longer exists, and an `unreviewed` count that differs from `FF_UNREVIEWED_BASELINE` in either direction (above: a new phase was parked; below: a reviewed row did not lower the constant in the same commit). It runs in the preflight step of both workflows and is selected by no `FF_` profile.

| Release responsibility | Existing required tags |
|---|---|
| CLI/user ownership and recovery intent, paths, partial state, receipts and no-op | `baseline-merge`, `hook-wiring-intent`, `wire-hooks-beside`, `bootstrap-baseline-hop`, `cli-0259`, `cli-flow-recovery`, `cli-flow-recovery-selectors` |
| Install, upgrade and provider delivery | `bootstrap-exception`, `upgrade-classify`, `upgrade-classify-eol`, `upgrade-boundary`, `upgrade-repair`, `n5-delivery`, `n6-truthful-base`, `n6-missing-base`, `n6-recover`, `cli-rendered` |
| Executable safety | `fixtures`, `git-smoke`, `interpreter-contract`, `python3-version`, `git-context`, `secret-scan-staged`, `trusted-enforcer`, `hook-install-rc`, `approval-binding`, `approval-writer`, `approval-schema3`, `command-policy` |
| Validator execution | `validator-evidence`, `validation-instructions` |
| Publication integrity | `release-authority`, `release-tag-binding` |
| Caller-summary truthfulness and publisher scoping | `hop-log-truth`, `n4-parity-scope` |
| Test-harness fidelity (a suite must exercise its own subject on both platforms) | `minimal-path-fixture` |
| Consumer-facing stamper and recovery-hint honesty | `stamp-eol-guard`, `recovery-hint` |
| Harness liveness — no command substitution may capture a git/hook process tree, and a block must be bounded at its own operation | `git-capture-guard` |
| Runner invocation preconditions — refused with rc 2 before any phase runs, and never in hosted CI | `run-preconditions` |

The reusable workflow separately requires the UNSCOPED `test-ff-only.sh` suite plus preflight, runner parity, both manifests, module size, mirror parity, public-surface allowlisting and a clean tree. That suite rejects phase failures, timeouts, missing phases, unauthorized `N/A` and zero-result success, AND asserts this allowlist's size and boundary tags. It runs as its own step, never inside `FF_RELEASE=1`: it drives the runner with `FF_ONLY`/`FF_FULL`, which the runner refuses to combine with `FF_RELEASE` (rc 2), so `ff-only` must stay OUT of the allowlist. Keeping it a separate step is also what makes the membership ratchet non-self-referential — dropping a tag cannot switch off the assertion that would have caught it. Full and change-scoped diagnostics remain callable; release selection does not delete or weaken them.

## Registered, not in the release profile

Every registered phase that is neither in `FF_RELEASE_TAGS` nor an opt-in diagnostic states its route here. `step-gated` names the `fusebase-flow-verify.yml` step that runs it outside `FF_RELEASE=1` (the step name is verified literally; that is the whole depth of step verification). `deferred` names the measurement that bought the deferral. `unreviewed (pre-ratchet, <date>)` is the seeded legacy backlog: the count may only shrink, the commit that reviews a row lowers `FF_UNREVIEWED_BASELINE` to match, and a NEW phase can never be parked here. Reviewing these rows is a separate outcome — each promotion carries an MSYS cost (`docs/backlog/gate-bounds-lack-headroom/`).

**2026-09-12 triage.** Every pre-ratchet row except the Process-lifecycle five was decided by reading the phase's own header and asking one question: does it carry a consumer or safety contract? The verdict is the first token of the Reason cell — `PROMOTE-CANDIDATE` (belongs in `FF_RELEASE_TAGS`; each still needs its own MSYS cost number, and none was promoted in that outcome), `OPT-IN-CANDIDATE` (belongs in the Diagnostic-exclusions table and `FF_OPTIN_TAGS`, which is a runner change), `SPLIT-FIRST` (one tag mixes an editorial instrument with a real delivery guarantee; promote the split, never the phase). All three read `deferred` because this table admits no other decided value: the classification column is the route, the Reason cell is the decision. The five left `unreviewed` are `health-check-timeout`, `job-probe`, `liveness`, `msys-tree-cleanup` and `ws5-upgrade` — the Process-lifecycle family, which needs its own look first (`health-check-timeout` carries a pre-existing Linux failure).

| Tag | Classification | Reason |
|---|---|---|
| `ff-only` | step-gated — `Runner selection, failure, timeout, zero-result and release-membership contracts` | Drives the runner with `FF_ONLY`/`FF_FULL`, which the runner refuses to combine with `FF_RELEASE` (rc 2); membership would redden it |
| `module-size` | step-gated — `Module-size ratchet (FR-25, full scan vs committed baseline)` | Self-test of the FR-25 checker that step runs with `--all` |
| `hook-manifest` | step-gated — `Hook-layer manifest freshness` | Self-test of the stamper/verifier pair that step runs |
| `fingerprint-rows` | step-gated — `Preflight (structure + YAML + frontmatter + mirror drift + action-name consistency)` | Self-test of preflight §10, which that step runs against the real tag history |
| `preboundary-consumed` | deferred | Green on both platforms but measured 204 s on MSYS (13 `bootstrap-upgrade.sh` engine hops across 9 fixture trees) against 5-56 s for every phase promoted in v4.16.3. Excluded on cost, not on coverage; reopen with the operator if the MSYS leg gains headroom |
| `approval-receipt` | deferred | PROMOTE-CANDIDATE — sole oracle for the FR-12 clone-durable approval receipt: a committed deploy report citing gitignored `state/approvals/<action>-<slug>-<date>.json` dangles on a fresh clone, and these rows prove the receipt replaces that citation without relaxing the gate. Every other FR-12 oracle (`approval-binding`, `approval-writer`, `approval-schema3`) already gates. Blocked on: one MSYS cost number |
| `boot-size` | deferred | SPLIT-FIRST — arms 1-2 are editorial byte ceilings owned by `decisions.md` A2, where changing a number is a decision amendment and never a test edit (diagnostic). Arms 3-4 are real delivery guarantees: frontmatter at byte 1, the anti-reread rule resident in BOTH mandatory skills, every required lazy `references/*.md` present, and no carrier falsely claiming a mandatory skill BODY is already in context — a false eager claim makes a mandatory skill silently never load. Promote the delivery arms as their own tag, not the phase |
| `cli-vendor` | deferred | PROMOTE-CANDIDATE — guarded CLI re-vendor oracle for the edition's strongest named data-loss vector (`docs/fusebase-cli-edition.md` section Two-writer hazard): a blind copy destroys a consumer's `CUSTOM:SKILL` block. Non-vacuous by construction — `R1c --blind` DESTROYS the same fixture R1 preserves. Install and distribution (provider delivery) has no release tag covering it. Blocked on: one MSYS cost number |
| `cli-version` | deferred | Consumer-visible health verdict on an incompatible CLI, with gate-neutered mutation controls. **Measured 2026-09-12 at `ae48484`: 441 s MSYS / 4 s Linux, 25/25 PASS on both.** Above the ~300 s promotion bar, and the cost is STRUCTURAL rather than host noise — ~8 `fusebase-flow-health-check.sh` engine spawns at ~55 s each, inside this file's own documented 40-70 s per-engine-run band (same-host reference that hour: `approval-schema3` 334 s). Cheaper route, also measured: `test-cli-version-gate.sh --only below-incompatible-0.25.16` is **149 s** and still carries all 20 parser/boundary/override rows, the ONE hard failure (`CLI_VERSION_UNSUPPORTED`, exit 1) with its mutation control, and the T87 `path_without_fusebase` guard. The 292 s it drops is the three ADVISORY arms (newer / unreadable / absent) plus their controls, which all assert the same HEALTHY-exit-0 observable. Promote that SELECTION as its own phase (precedent: `cli-flow-recovery-selectors`), not the whole phase. **T87 guard gating status: still UNGATED.** The v4.16.1 `path_without_fusebase` surviving-toolchain fix runs in no CI profile while this row stays deferred. It was executed on the failing platform for the first time in this outcome: with a `fusebase` planted in `/usr/bin` of `ff-gate:24.04` it correctly halts the phase at `FAIL: cli-version base-path-keeps-the-toolchain` rc 1 instead of greening 25 vacuous rows |
| `codex-parity` | deferred | PROMOTE-CANDIDATE — Codex command/provider delivery: the AGENTS.md equivalents table, the installer's single-sourced transform proven RED-then-GREEN against a canonical body edit, and refuse-to-overwrite-unmarked. `Install and distribution` requires command/provider delivery coverage and no release tag reaches the Codex surface. Blocked on: one MSYS cost number; promote with `codex-plugin` |
| `codex-plugin` | deferred | PROMOTE-CANDIDATE — Codex plugin surface manifest checked against the canonical skill and both mirrors. Static reads, no engine spawn, so the MSYS cost is near zero. Same provider-delivery gap as `codex-parity`; promote the pair together |
| `denial-message` | deferred | OPT-IN-CANDIDATE with one reservation — the DENY behaviour is already gated by `command-policy`; this phase asserts the explanation TEXT, which is the Diagnostic-exclusions shape. The reservation is its negative invariant (the message must claim NO location, because attributing a span needs the shell-parsing reserve): that one is a safety constraint, not wording. Blocked on: deciding whether the invariant is release-critical alone, else move the row to Diagnostic exclusions and `FF_OPTIN_TAGS` |
| `fr22-delivery` | deferred | SPLIT-FIRST — the template arm asserts prose presence (editorial); the `stop.py` arm is a live shipped-hook contract proven RED-then-GREEN, including decision D1's warn-not-deny semantics for a recommended-missing signal. Same disposition as `boot-size`: promote the live arm, not the phase |
| `git-context-mutation` | deferred | PROMOTE-CANDIDATE — mutation oracle for `git-context`, which IS in the release profile. A gated phase whose discriminating power is proven only by an ungated oracle gates a row set nobody has shown can fail. Blocked on: one MSYS cost number for the trio with `interpreter-mutation` and `python3-version-mutation`, which are the same shape |
| `health-check-timeout` | unreviewed (pre-ratchet, 2026-09-10) | Health-check timeout propagation; Process lifecycle row, which has zero release tags |
| `install-doc` | deferred | PROMOTE-CANDIDATE — executable ownership contract over `docs/install-fusebase-cli-project.md`: every path the doc tells an operator to copy exists, and the Bash and PowerShell blocks name the same source set. Its originating defect was consumer-facing and silent — the PowerShell block copied the retired `skills/` dir after the canonical dir moved to `flow-skills/`, so a Windows operator following the shipped procedure installed ZERO Flow skills while the Bash block at the same step was correct. Static reads; near-zero MSYS cost |
| `interpreter-mutation` | deferred | PROMOTE-CANDIDATE — mutation oracle for `interpreter-contract`, which IS in the release profile; identical argument and identical shape to `git-context-mutation`. Promote the trio together |
| `job-probe` | unreviewed (pre-ratchet, 2026-09-10) | Job-probe honesty; Process lifecycle row, which has zero release tags |
| `lane-router` | deferred | OPT-IN-CANDIDATE — FR-21 lane classification is advisory; the lane safety floor is enforced by role discipline and the commit gates, not by this router. It reaches only maintainer `Focused contracts (not release evidence)`, which is feedback, not a release route. Blocked on: the runner change (Diagnostic-exclusions table plus `FF_OPTIN_TAGS`), not taken in this outcome |
| `lane-workflow` | deferred | OPT-IN-CANDIDATE — instruction-shape assertions over the Lightweight-lane workflow and its record schema, not a runtime behaviour a consumer can observe break. Same disposition and same blocker as `lane-router` |
| `liveness` | unreviewed (pre-ratchet, 2026-09-10) | FR-27 bounded-run behavior; Process lifecycle row, which has zero release tags |
| `msys-tree-cleanup` | unreviewed (pre-ratchet, 2026-09-10) | Owned-child cleanup on MSYS; covered by nothing in the release gate today |
| `newline-preserve` | deferred | PROMOTE-CANDIDATE — AC2 of `sync-version-strings.sh`: the previous write primitive stripped each file's EOF newline and churned consumer docs on every version sweep, a consumer-visible artifact defect. Self-contained temp repo, no engine spawn; it is already the suite's cheap deterministic fixture phase, so promotion costs almost nothing |
| `po-investigate` | deferred | PROMOTE-CANDIDATE (safety) — `hooks/local/po-investigate.sh` forwards args to git diff/log/show, which accept args that WRITE FILES (`--output=`) or RUN EXTERNAL PROGRAMS (`--ext-diff` with `GIT_EXTERNAL_DIFF`, a pager, an editor). The RED arm reconstructs the pre-fix wrapper from git history and proves the escape vector was live; the GREEN arm proves the shipped wrapper refuses it with no file written. Nothing in the release profile covers a read-only claim that can execute. Blocked on: one MSYS cost number |
| `po-verifiable-boot` | deferred | OPT-IN-CANDIDATE — byte-identity and drift arms over the two PO command copies and the canonical agent are editorial, and the two live-handler arms overlap what `fixtures` already drives through `user_prompt_submit.py` / `stop.py`. Same disposition and same blocker as `lane-router`; revisit if either handler arm is shown unique |
| `policy-state` | deferred | PROMOTE-CANDIDATE (ownership) — a project's `workflow_mode` / `worker_undisturbed` override, held in a `*.local.yml` deep-merged by `policy_loader`, must survive an upgrade that refreshes `policies/` wholesale. That is the `user/CLI byte preservation and supported upgrade paths` row directly, and losing it silently re-points a consumer's protected paths. Blocked on: one MSYS cost number |
| `prohibition-residency` | deferred | OPT-IN-CANDIDATE — ledger plus a self-declared HEURISTIC drift net over prose (decisions A3: a prohibition parked in a lazy `references/*.md` is only loaded by an agent who already remembered it). Prose-searching with a heuristic arm is exactly the Diagnostic-exclusions shape. Same disposition and same blocker as `lane-router` |
| `python3-version-mutation` | deferred | PROMOTE-CANDIDATE — mutation oracle for `python3-version`, which IS in the release profile; identical argument and identical shape to `git-context-mutation`. Promote the trio together |
| `signal-reap` | deferred | Reviewed at T4 of `docs/backlog/harness-kill-leaves-orphan-children/`: the RED baseline is CLOSED — **10/10 on MSYS**, zero surviving descendants across five outer-wall kills and zero fixture leftovers, all by process inspection. **Cost measured: 166 s on a quiet MSYS host** (9-process table), 342 s on a contaminated one; Linux is statically `N/A` at 18 s and costs nothing. That CLEARS the cost bar (`preboundary-consumed` excluded at 204 s, `approval-schema3` carried at 184-286 s), and it is the only oracle for the Process-lifecycle owned-child row, which has zero release tags. It is deferred on ONE remaining objective condition, not on cost: it has never run green on HOSTED Windows, because it was red at every commit until now — so promoting it would make the next tagged gate its first hosted execution. Close that with one non-publishing `.github/workflows/fusebase-flow-measure-windows.yml` run, then promote |
| `sync-allowlist` | deferred | PROMOTE-CANDIDATE — under-reach guard on the version-string sweep: a token-bearing framework file omitted from the allowlist would silently never sync, which is the v4.6.0 red-main and the GEMINI-stuck-at-v2.1 shape. It derives the TRUE set from the tree instead of trusting the list, and self-verifies that it would catch an omission. Static; near-zero MSYS cost |
| `token-waste-classify` | deferred | OPT-IN-CANDIDATE — auto-classification contract for an advisory instrument that ships no consumer-visible behaviour. Same disposition and same blocker as `lane-router` |
| `wasted-effort-windowing` | deferred | OPT-IN-CANDIDATE — self-test of `find-wasted-effort.py` windowing, an editorial instrument. Same disposition and same blocker as `lane-router`. TRIPWIRE if it is ever promoted: it spawns a hardcoded bare `python3`, so on a hosted Windows leg it depends on that workflow's own `python3` shim rather than on `${PYTHON}` |
| `ws5-upgrade` | unreviewed (pre-ratchet, 2026-09-10) | Bounded upgrade WS5; Process lifecycle row, which has zero release tags |

## Diagnostic exclusions

These checks remain available with `FF_ONLY` when their subject changes. They do not add unique runtime protection to every release. This table authorizes the T63 runner classification; no existing supported behavior is removed.

| Tag | Why opt-in | Required protection retained |
|---|---|---|
| `return-budget` | Checks exact editorial wording of delegated response limits | Delivery/preflight and actual hook controls; response-size guidance remains shipped |
| `supersede-primitive` | Searches prose for editing advice and retired phrases | Actual recovery preservation/idempotency tests; editorial review on instruction changes |
| `rule-inventory` | Instrument for deliberate rule-compression comparisons | Boot/delivery/prohibition checks; run inventory explicitly for rule changes |
| `startup-context` | Frozen compression baseline and size comparison | boot-size, prohibition-residency, provider delivery and mirror checks |
| `budget-literals` | Consistency of performance-budget numbers in live prose | boot-size retains the implemented structural/size checks |
| `history-extraction` | One-time migration equivalence against historical Git blobs | Current rule/skill structure and delivery; Git preserves migration history |
| `consumer-benchmark` | Comparative profiling/benchmark output | Actual recovery, ownership, no-op and failure scenarios in cli-flow-recovery |

Exact machine-consumed markers, schema keys and public command names remain valid assertions. Prose synonyms and numbered comments are not executable contracts. `release-authority` validates the parsed job graph; `validation-instructions` exercises configured validator execution instead of matching guidance sentences. Broader validator-evidence tests continue to cover unavailable reuse and failure propagation.

## Execution and completion

Aim for seconds to two minutes in the edit loop and a focused hosted check within ten minutes per platform. These are design targets, not measured guarantees or reasons to skip a critical test. Release checks retain their committed bounds until evidence supports a deliberate redesign.

`approval-schema3` is the most expensive release-profile phase: measured **184-286 s on MSYS** (the spread is concurrent load on the measuring machine; budget the upper figure) and **9-14 s on Linux** (25 rows, each a throwaway repository driven through the real writer, gate, handler and `pre-push` hook; the shared harness lives in `hooks/tests/fixtures/approval_fixture.py`, which the runner's `fixtures/*.json` glob ignores). It is in the profile because it is the only oracle for the FR-12 command-approval contract, but the cost sits beside `preboundary-consumed`, which was excluded at 204 s — reclassify deliberately if the MSYS leg loses headroom (`docs/backlog/gate-bounds-lack-headroom/`).

After a change, run its affected group once. Repeat only after a relevant correction or to investigate a named nondeterministic condition. A failed execution never becomes a passing result because earlier rows passed. Preserve the failing diagnostic, fix its owner and rerun the affected group; do not replay an unrelated successful prefix. Before tagging, run newly promoted phases and changed platform-dependent cases on Linux and MSYS.

Only release CI on the exact tagged SHA authorizes a release claim. Do not run the same unchanged full verification before tagging and again after tagging. The tag-triggered workflow runs the essential profile and package checks once on Linux and Windows/MSYS, then publishes only after the aggregate gate succeeds. Maintainer feedback is a focused subset and cannot authorize publication. No test-result signing/cache system is needed; a normal run records its source, platform, command, result and log pointer.

In this repository the runner rejects local `FF_FULL`/`FF_RELEASE` without a nonempty `FF_EVIDENCE_GAP`; hosted CI, including the release gate, is exempt, and a consumer tree without this file never sees the check. The gap names why affected groups cannot answer the question. The runner prints the reason but cannot judge it: the guard makes expansion deliberate, not justified.
