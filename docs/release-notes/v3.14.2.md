# Fusebase Flow v3.14.2 — doc-consistency sweep

**Release date:** 2026-06-07
**Previous version:** 3.14.1

## Summary

A documentation-consistency polish pass (the "repo polish" the operator requested). It corrects stale **prose** counts and canonical-path references that the automated `sync-version-strings.sh` sweep deliberately does not touch (it only rewrites attestation/banner strings + parenthesized skill counts). No behavior, rule, or model change.

## Fixes

| File | Was → Now |
|---|---|
| `audit/README.md` | "14 canonical Flow skill mirrors" → **27** |
| `docs/fusebase-cli-edition.md` | "14 canonical Flow skills" → **27** |
| `docs/source-map.md` | "14 canonical … `skills/<slug>/`" → **27 canonical … `flow-skills/<slug>/`**; "28 Flow mirror" → **54**; skill-catalog + clean-room path refs → `flow-skills/` |
| `docs/clean-room.md` | attestation-scope canonical paths `skills/` → **`flow-skills/`** (3 refs) |
| `PUBLISHING.md` | "hook tests: 14 / 14" → **16 / 16**; "18 files mirrored" → **54** |
| `docs/compatibility.md` | (already corrected in v3.14.1: 27 skills / 54 mirrors / 16 tests / `flow-skills/`) |

## Other languages (i18n)

The 6 translated READMEs (`docs/translations/{de,es,fr,ja,pt-BR,zh-Hans}/README.md`) were audited. They are **intentionally version-free summaries** that defer to the canonical English README for "complete, always-current" details — so they carry no version numbers, skill counts, or paths and need no per-release edits. This is the correct i18n pattern (no translation drift). **Left unchanged by design.**

## Version

`.claude-plugin/plugin.json` / README badge / `VERSION` → 3.14.2; live attestation strings swept to v3.14.2; `FLOW_RULES.md` Status `v0.13 → v0.14` + amendment entry.

## Verified

- preflight: 0 errors / 0 warnings (incl. §8 command-surface guard; plugin.json == VERSION == 3.14.2)
- run-tests: 16 / 16 PASS
- repo-wide audit: no remaining stale canonical-path (`skills/`) or count (`14`/`25`/`26`/`28`/`18`) references in live (non-historical, non-translation) files

## Rollback

`git revert <SHA>` — single commit; markdown/json/shell text only, no schema/data/policy change. Tag `v3.14.2`.
