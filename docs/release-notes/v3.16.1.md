# Fusebase Flow v3.16.1 — public roadmap + parked backlog

**Release date:** 2026-06-10
**Previous version:** 3.16.0

## Summary

Docs-only patch. Publishes the project's **public roadmap** and the two parked backlog tickets that previously existed only on a stranded, never-pushed local branch (pre-v3.2 line, commit `d8f24f5`) — the published repo had no roadmap at all. All content rewritten to the v3.16.0 baseline rather than copied stale. No rule or behavior change.

## What changed

| Surface | Change |
|---|---|
| `ROADMAP.md` (new, root) | Released arc v3.2→v3.16 · current shape (25 FR rules / 28 skills / 22 hook tests) · **Next likely:** architect sub-agent, role × path hook enforcement · **radar:** rail-mapping FR-20..25 rows, `.claude/commands` refresh path, FR-25 baseline rename handling, dogfood baseline, new provider surfaces · corrected non-goals (plugin + slash commands = optional conveniences; regex gates only for objectively countable rules). |
| `docs/backlog/architect-sub-agent/` + `docs/backlog/role-path-hook-enforcement/` (new) | Harvested + refreshed: `flow-skills/` paths, 22/22 test baseline, `docs/tmp/handoff` relay model, FR-25 `_glob_to_regex` reuse noted (de-risks the glob unknown), `policies/*.local.yml` gitignore note. `docs/backlog/index.md` created (3 parked tickets). |
| `.github/workflows/fusebase-flow-verify.yml` | `ROADMAP.md` added to the public-surface allowlist (the documented approval path for a new top-level surface). |
| `README.md` + `CONTRIBUTING.md` | Roadmap pointers (public-docs list; before-you-start non-goals check). |
| repo-wide | Attestation strings swept to v3.16.1; `FLOW_RULES.md` Status `v0.16 → v0.17` + amendment entry (no rule text changed); badge + plugin.json + VERSION → 3.16.1. |
| housekeeping (local-only) | Local `main` fast-forwarded to `origin/main`; the stranded 9-commit line archived as local branch `archive/universal-line-pre-v3.2`. The stale-local-main hazard is resolved. |

## Verified

- preflight: 0 errors / 0 warnings (incl. §8; plugin.json == VERSION == 3.16.1)
- run-tests: 22/22 PASS
- CI green on `ad1fb7f` + `2b02db7` including the public-surface guard with the new root file

## Rollback

`git revert <SHA>` — docs + one allowlist line + version metadata. Tag `v3.16.1`.
