# Fusebase Flow v3.12.1 — FR-23 wiring completeness

**Release date:** 2026-06-07
**Previous version:** 3.12.0

## Summary

A corrective patch over v3.12.0 (FR-23 documentation budget). An independent post-release review found that several doc-creating skills and one always-on baseline were not fully wired to FR-23. No new rule; FR-23 semantics unchanged. Formal handoff relays intentionally remain under `docs/handoff/` for this patch (see Deferred).

## Fixes

| Surface | Fix |
|---|---|
| `GEMINI.md` | Stale baseline (`v3.11.1` / `FR-01..FR-22`, no documentation-budget) swept to `v3.12.1` / `FR-01..FR-23`. The AGENTS/CLAUDE/GEMINI always-on trio is now consistent. (FLOW_RULES.md was already correct.) |
| `flow-skills/requirements-specification/SKILL.md` | Stale `skills/lightweight-lane/SKILL.md` → `flow-skills/lightweight-lane/SKILL.md`; added an FR-23 documentation-budget pre-write classifier — Tier 0/1/2 create no spec artifacts; only Tier 3/4 draft a full spec. |
| `flow-skills/implementation-planning/SKILL.md` | Added an FR-23 documentation tier gate: `decisions.md` only when a real decision exists; `verification-gate.md` only when lane/policy requires; the implement handoff points to canonical `spec.md`/`decisions.md`/`tasks.md` and must not reprint them. Fixed two stale `skills/` references → `flow-skills/`. |
| `flow-skills/communication/SKILL.md` | Mode-B prose intro now lists `docs/tmp/handoff.md` (active restart state) alongside `docs/handoff/` (formal relays); the file-classification block already listed both. |
| `flow-skills/product-docs-first/SKILL.md` | Gating extended: also skip product-doc expansion for already-scoped implementation work. |
| `flow-skills/business-logic-guardian/SKILL.md` | Guards on **either** `docs/<app>/business-logic-index.md` (AI-default) **or** `docs/<app>/business-logic.md` (human narrative); the index is the primary guard layer when both exist. |
| Canonical baselines | FLOW_RULES/AGENTS/CLAUDE/GEMINI version strings → v3.12.1; mirrors regenerated (52 = 26 × 2); manifest updated. |

## Deferred (intentional)

- **Formal handoff relay path.** Operator preference is to host *all* handoffs (active + formal) under `docs/tmp/handoff`. Formal implement/deploy relays currently live at `docs/handoff/*` and are wired into the **deploy-safety gate** (`policies/required-artifacts.yml` `before_deploy_command`, `policies/gate-contracts.yml` smoke pattern) plus ~18 workflow/agent/template files and hook fixtures. Migrating them is a deploy-safety-sensitive change, not a small patch — tracked as a separate ticket pending operator go-ahead. Active session continuity already uses `docs/tmp/handoff.md`.
- **Repo-wide attestation sweep** (`hooks/local/sync-version-strings.sh`): workflows/agents/templates + most docs still read `v3.11.1` / `FR-01..FR-22`; separate follow-up.

## Verified

- preflight: 0 errors / 0 warnings
- run-tests: 16 / 16 PASS
- skill mirrors: 52 files (26 × 2) byte-identical to canonical; manifest consistent
- `templates/business-logic.md` still unchanged (preserved human-narrative option)

## Rollback

`git revert <SHA>` — single commit; reversible (markdown + skill text only; no schema/data/policy change). Tag `v3.12.1`.
