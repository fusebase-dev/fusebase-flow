# FuseBase Flow v3.30.3 — Windows/MSYS + adoption-path hardening (WS1-WS9)

**Release date:** 2026-07-01
**Previous version:** 3.30.2
**Type:** PATCH (hardening across nine workstreams + adversarial-review corrections)

## Summary

Hardens the Windows/MSYS runtime and the adoption path across **nine workstreams (WS1-WS9)** plus the corrections folded from two independent adversarial reviews (Codex + FuseBase workflow, both SHIP on the full corrected diff). No new FR rule (FR-01..FR-27 unchanged) and no new Flow skill (32 unchanged). Additive / FR-07-clean: no FR rule rows, no deploy-policy rule semantics, and no `ratchet-governance.yml` touched — only the FLOW_RULES version-attestation line moved.

## What ships

| WS | What it does |
|---|---|
| **WS1 — secret-scan + protected-path adoption** | Secret-scan handles runtime tokens; a single-use, digest-bound protected-path exception path; a safe git-hook (re)install path. |
| **WS2-core / WS3 — MSYS bounded run + harness** | Strict winpid scoping for the MSYS bounded run; test-harness reap; the fixture-phase stdin fix so the fixture phase completes under the bounded wrapper. |
| **WS4 — health-check verdict robustness + MSYS timeouts** | A killed/unparseable hook-test run reclassifies to an advisory verdict instead of a false BROKEN; MSYS-tuned timeout defaults. |
| **WS6 — backward-compatible dual-marker migration** | The overlay heading marker accepts the legacy `## Fusebase Flow —` and the new `## FuseBase Flow —` form, so preflight and health-check agree on a valid overlay. |
| **WS7 — problem catalog** | `docs/problem-catalog/` seeded as the recurring-problem record of truth. |
| **WS8 — zero-trust liveness (FR-27 extension)** | The zero-trust sub-agent liveness rule codified: poll git-progress / process, never passively wait on a completion ping. |
| **WS9 — slash-command naming** | Command naming aligned across surfaces. |
| **Review corrections** | Glob-bypass close, unique hook marker, trap re-verify, test fidelity. |

## Clean-room

No third-party code, prompts, skill/command/hook text, docs, or dependency copied or vendored; prior clean-room state intact (`git grep -ni headroom` in code = 0).

## Verified

preflight **0/0** · `check-module-size --all` exit 0 · mirror-skills **32 skills / 0 drift** · manifest **86/86/0-dups** · plugin == VERSION == **3.30.3** · README badge 3.30.3 · the POSIX `run_with_timeout` path preserved · `git grep -ni headroom` in code = 0. FR range FR-01..FR-27, 32 skills unchanged.

### Deploy smoke (LIGHT — loaded-host discipline)

preflight 0/0 + health-check + tag/release verification. Full `run-tests.sh` on a quiet MINGW64 box is a **consumer-verify** item — the fixture phase now passes under the bounded wrapper.

## Rollback

`git revert <release commit>` (release commit `989604e`, tag `v3.30.3`) — hardening + docs; re-push; re-mirror.
