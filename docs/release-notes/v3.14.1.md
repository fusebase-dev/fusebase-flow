# Fusebase Flow v3.14.1 — release-hygiene polish

**Release date:** 2026-06-07
**Previous version:** 3.14.0

## Summary

A small consistency patch over v3.14.0. **The handoff model is unchanged** (directionally approved): the `handoff` skill writes active restart state to `docs/tmp/handoff.md`, uses FR-18 supersede-in-place and FR-23 pointers-not-reprints, and stays separate from the formal implement/deploy/architect relays. This release only corrects surface metadata and adds a guard so those surfaces can't silently drift again.

## Fixes

| # | Fix |
|---|---|
| 1 | **`/handoff` vs `handoff` skill clarified.** `/handoff` is the **Claude Code** slash command; the `handoff` **skill** is the portable cross-agent workflow. `AGENTS.md` now states the non-Claude path explicitly — on Codex / Cursor / Copilot / Gemini, *invoke the `handoff` skill and write `docs/tmp/handoff.md`* (same skill, same output, no command). `CLAUDE.md` bullet clarified too. |
| 2 | **`.claude-plugin/plugin.json`** version `3.10.0` → `3.14.1`. |
| 3 | **README** version badge `3.11.1` → `3.14.1`. |
| 4 | **`docs/compatibility.md`** refreshed: **27** Flow skills (was 14), mirror count **54 = 27 × 2** (was 28 = 14 × 2), hook tests **16/16** (was 14/14), canonical source **`flow-skills/`** (was `skills/`), `/handoff` listed. |
| 5 | **README existing-repo copy block** fixed: `cp -R $SRC/flow-skills ./` (was `cp -R $SRC/skills ./`). |
| 6 | **`preflight.sh` §8 (new) — command-surface consistency guard.** Fails preflight unless: `.claude/commands/handoff.md` exists, `CLAUDE.md` lists `/handoff`, `AGENTS.md` explains the portable (non-Claude) invocation, and `.claude-plugin/plugin.json` version == `VERSION`. |

Plus: version-string sweep brought live attestation strings to v3.14.1; `FLOW_RULES.md` Status `v0.12 → v0.13` + amendment entry.

## Not changed

The handoff skill, template, `/handoff` command behavior, FR-23, and the `docs/tmp/handoff` path model are all untouched. No architecture migration.

## Verified

- preflight: 0 errors / 0 warnings (with new §8 active)
- §8 guard: negative test (plugin version mismatch) → preflight ERROR; positive → clean
- run-tests: 16 / 16 PASS

## Rollback

`git revert <SHA>` — single commit; markdown/json/shell text only, no schema/data/policy change. Tag `v3.14.1`.
