# Fusebase Flow v3.18.1 — post-ship nit-sweep (waiver-path consistency + restatement dedup)

**Release date:** 2026-06-11
**Previous version:** 3.18.0

## Summary

Closes everything the independent post-ship audit of v3.18.0 found (that diff's first independent review — verdict: **0 blockers**, 9 nits + 3 pre-existing finds). No behavior or enforcement change.

## What changed

| Fix | Detail |
|---|---|
| Waiver-path consistency (N1/N2/N9) | The DP.6 confirmation prompt no longer claims "Approval artifact verified" before the artifact exists on `dp1_waiver: eligible` deploys — it branches (verified / waiver-eligible, stamp-on-phrase) and the stamp step is explicit in the response line; mandatory-read #7 and `release-deploy-reporting` step 1 carry the waiver branch |
| Restatement dedup (N4) | Gate-field lists in the ai-developer agent (6c row) + role-reference IM.9 → pointers at `gate-contracts.yml` / `gate-report.md`. The agent's copy had **already drifted** (missing `implementation_summary`) — live proof of the hazard v3.18.0 targeted |
| G2 producer line | Omitting `decisions.md` now requires recording the literal `no real decisions` in spec.md's Clarify summary — the marker `required-artifacts.yml: optional_when` and the greenlight checklist consume |
| Canonical handoff drafting | `implementation-planning` step 12 and `release-deploy-reporting` step 4 repointed from "handoff-folder-README shape" to the canonical `templates/handoff-implement.md` / `handoff-deploy.md` |
| Residue (N3/N5/N6/N7/N8 + pre-existing) | Table-breaking `\|` in the waiver row; README :230/:334 conditional security review; carrier count 5→7 corrected in same-release notes; `task_complete` removed from the event-schema enum (Codex example verified not wiring it); §5d label; gate-report placeholder attestation made sweep-maintained; `required-artifacts.yml` header names its real consumer (stop.py only) |

## Verified

preflight 0/0 · run-tests 24/24 · `--all` green · sweep clean · event schema valid JSON · mirrors 0 drift.

## Rollback

`git revert <SHA>` — text-only. Tag `v3.18.1`.
