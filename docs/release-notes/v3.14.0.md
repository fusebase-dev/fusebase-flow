# Fusebase Flow v3.14.0 — handoff procedure finalized + version-string sweep

**Release date:** 2026-06-07
**Previous version:** 3.13.0

## Summary

Two things, both finishing work started earlier in the v3.12–v3.13 line:
1. **Handoff procedure finalized** — a dedicated `handoff` skill + `/handoff` slash command + a 16-section template, completing the *active-continuity* half of FR-23 Tier 2 (the formal implement/deploy relays already moved under `docs/tmp/handoff/` in v3.13.0).
2. **Version-string sweep** — the deferred mechanical hygiene: every live attestation/banner string now reads the current version, FR-range, and skill count.

No FR added or removed; FR-23 semantics unchanged.

## Added

| Surface | What |
|---|---|
| `flow-skills/handoff/SKILL.md` (new) | Operator-triggered skill (`invocation: manual`). Writes the active session restart state to `docs/tmp/handoff.md` for the next AI coding agent — 16 sections, Mode B, supersede-in-place (FR-18), pointers-not-reprints (FR-23). Explicitly distinct from the formal `docs/tmp/handoff/<date>-<slug>-{implement,deploy}.md` relays. |
| `templates/handoff.md` (new) | The 16-section Mode B substrate (Session Role · Goal · Current State · Active Files in Flight · Changed This Session · Key Decisions · Constraints · Failed Attempts · Known Issues · Next Step · Validation Plan · Relevant Commands · Environment · Dependencies · Risks · Completion Criteria). |
| `.claude/commands/handoff.md` (new) | `/handoff` slash command (4th command, alongside `/fusebase-health`, `/onboard`, `/product-owner`). |

## Version-string sweep (deferred hygiene, now done)

`hooks/local/sync-version-strings.sh` rewrote the live attestation/banner/FR-range/skill-count strings to **v3.14.0 / FR-01..FR-23 / 27 skills** across root adapters (AGENTS/CLAUDE/GEMINI/FLOW_RULES), `.github`/`.cursor`, canonical agents + skills, workflows, templates, overlays, and top-level framework docs. History is preserved — CHANGELOG, `docs/release-notes/**`, `docs/specs/**`, and the `docs/handoff/**` + `docs/tmp/handoff/**` dated archives are pruned from the scan, so provenance strings ("since v2.4.0", etc.) survive.

## Catalog / counts

- Canonical skill count **26 → 27**; mirrors regenerated (54 = 27 × 2); manifest refreshed.
- `CLAUDE.md` / `AGENTS.md` skill catalogs + `/handoff` added; `FLOW_RULES.md` Status `v0.11 → v0.12` + amendment entry.
- README corrected: Flow skills 25 → 27 (+ catalog rows for documentation-budget and handoff), templates 14 → 24.

## Verified

- preflight: 0 errors / 0 warnings
- run-tests: **16 / 16 PASS** (deploy-gate fixtures unaffected)
- skill mirrors byte-identical to canonical; manifest consistent
- the `handoff` skill references the single `templates/handoff.md` (no duplicated template — FR-23)

## Rollback

`git revert <SHA>` — single commit; markdown + skill/template/command text only, no schema/data/policy change. Tag `v3.14.0`.
