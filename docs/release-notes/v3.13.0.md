# Fusebase Flow v3.13.0 — handoff artifacts consolidated under docs/tmp/handoff

**Release date:** 2026-06-07
**Previous version:** 3.12.1

## Summary

All handoff artifacts now live under `docs/tmp/handoff`. Handoffs are operational/transient AI-workflow artifacts, not durable product docs, so both active continuity and formal role relays belong in the same transient location. This was deferred from the v3.12.1 patch because formal relays are load-bearing for the deploy-safety gate; it is done here atomically in a dedicated release with full gate validation.

## Path model

```
docs/tmp/handoff.md                              active restart state (single file, superseded each session)
docs/tmp/handoff/<date>-<slug>-implement.md      formal implement relay (when needed)
docs/tmp/handoff/<date>-<slug>-deploy.md         formal deploy relay (when needed)
docs/tmp/handoff/<date>-<slug>-architect.md      formal architect relay (when needed)
```

`docs/tmp/` is git-tracked → the FR-04 audit trail is preserved.

## The change

| Surface | Change |
|---|---|
| `policies/required-artifacts.yml` | `before_deploy_command` path_glob `docs/handoff/*-deploy.md` → `docs/tmp/handoff/*-deploy.md`; `smoke_results_present` signal → `docs/tmp/handoff/*-smoke/`. |
| `policies/gate-contracts.yml` | smoke-dir pattern `^docs/handoff/...-smoke/$` → `^docs/tmp/handoff/...`. |
| `hooks/handlers/stop.py` | smoke detection regex → `docs/tmp/handoff/.*-smoke/`. |
| `hooks/tests/fixtures/13,14` | deploy-complete fixtures' transcript paths → `docs/tmp/handoff/...` (gate fixtures stay green). |
| workflows / agents (+ `.claude`,`.codex` mirrors) / templates / flow-skills (+ mirrors) | all `docs/handoff` references → `docs/tmp/handoff`. |
| `FLOW_RULES.md` | FR-23 row + implication updated to `docs/tmp/handoff/*`; Status `v0.10 → v0.11` + amendment-log entry. Rule set otherwise unchanged. |
| `AGENTS.md` / `README.md` / `.cursor` / `.github` / live docs | references updated. |
| `hooks/local/sync-version-strings.sh` | prune list note + explicit `docs/tmp/handoff` entry (dated relays protected from the version-string sweep). |
| `docs/handoff/` | retained as a frozen historical archive; README redirects to `docs/tmp/handoff/`. |
| baselines + VERSION | FLOW_RULES/AGENTS/CLAUDE/GEMINI + VERSION → v3.13.0. |

## Preserved history (not rewritten)

CHANGELOG, `docs/release-notes/*`, `docs/specs/*`, `docs/changes/*`, the FLOW_RULES amendment log, and the existing dated artifacts in `docs/handoff/`.

## Deferred (unchanged)

The repo-wide `sync-version-strings.sh` attestation sweep — workflows/agents/templates/most docs still carry older `v3.1x` / FR-range live strings; canonical baselines are current. Separate follow-up.

## Verified

- preflight: 0 errors / 0 warnings
- run-tests: **16 / 16 PASS** — deploy-gate fixtures (incl. 13/14 cleanup-marker and 15/16 lightweight) green after the policy/handler/fixture rewire
- skill + agent mirrors regenerated; manifests consistent
- no stray live `docs/handoff` references (remaining mentions are historical or the intentional prune list / archive redirect)

## Rollback

`git revert <SHA>` — single commit; markdown/yaml/py/json text only, no schema/data. Tag `v3.13.0`. (Reverting restores the v3.12.1 `docs/handoff` formal-relay wiring atomically, including the deploy gate.)
