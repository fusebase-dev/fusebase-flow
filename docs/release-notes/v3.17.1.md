# Fusebase Flow v3.17.1 — post-ship audit sweep (references drift gate + pointer nits)

**Release date:** 2026-06-10
**Previous version:** 3.17.0

## Summary

An independent post-ship audit of today's whole v3.16.0→v3.17.0 chain returned **ALL CORRECT — zero blockers** (remote state, tags, CI, gates, the unreviewed post-review fixes, overlay byte-identity, 55-rule split integrity, fresh-session bootstrap with no dangling reference, history-falsification guard held). This patch closes what it did find: residual nits and one real structural gap.

## What changed

| Finding | Fix |
|---|---|
| **Gap: `references/*.md` had no drift gate** — the per-role don't-lists (all 55 rules) moved there in v3.17.0, but `preflight.sh` §5 and the mirror manifest hashed only `SKILL.md` files; future mirror drift would have passed silently | `mirror-skills.sh` now hashes + manifests every references file (manifest 56 → 68 entries) with drift counting; `preflight.sh` §5 verifies `flow-skills/*/references/*` across both mirrors |
| 6 residual stale pointers ("don't-lists live in SKILL.md") | Repointed to `references/<role>.md`: `skill-authoring` ×2 (one also carried the retired `skills/` path), both agents' context-load tables, the claude overlay's mandatory-skill bullet (canonical edited → inline re-spliced, byte-verified), `violation-recovery`, `operator-discipline`, the parked architect-sub-agent ticket |
| PUBLISHING.md stale facts | Expected mirror output 56 → 68; inline public-surface allowlist synced to the live CI list (was missing `ROADMAP.md`, `.claude-plugin`, `flow-skills`; still listed retired `skills`) |
| Installer description | "copies `skills/`" → `flow-skills/` |

## Verified

preflight 0/0 (references drift checks active) · run-tests 24/24 · `--all` green · sweep clean · CLAUDE inline overlay byte-matches canonical · amendment-log guard held.

## Rollback

`git revert <SHA>` — docs/script text only. Tag `v3.17.1`.
