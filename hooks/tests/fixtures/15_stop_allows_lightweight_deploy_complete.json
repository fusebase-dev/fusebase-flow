{
  "_test": "Stop hook ALLOWS a Lightweight-lane (FR-21) deploy-complete claim without the Full-lane probes table / post-deploy docs commit / smoke — as long as the safety floor (deploy hash + rollback) is present",
  "_handler": "stop.py",
  "_expected_decision": "allow",
  "event": "stop",
  "session_id": "test-15",
  "cwd": ".",
  "host_tool": "claude_code",
  "agent_message": "Deploy complete (Lightweight lane, change_tier: lightweight). Dropped JSON pretty-printing; observed payload 10.6KB smaller, now under the cap (expected: under cap) ✓. FR-07 protected-path re-check clean. Deploy hash: deployed via abc1234ef. Rollback: git revert abc1234ef."
}
