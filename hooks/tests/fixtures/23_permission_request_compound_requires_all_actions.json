{
  "_test": "compound command with NO artifact denies (K8 all-match, no-artifact path). NOT a first-match discriminator — this fixture is denied by the old code too; the discriminating case (first action SATISFIED, deny hinges on the second) runs through both handlers in hooks/tests/test-command-policy.sh, because run_hook_tests.py executes every fixture from the repo root and a fixture cannot carry its own state/approvals/.",
  "_handler": "permission_request.py",
  "_expected_decision": "deny",
  "_expected_rule_id": "FR-12",
  "event": "permission_request",
  "session_id": "test-23",
  "cwd": ".",
  "host_tool": "codex",
  "permission_request": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "fusebase deploy && npx prisma migrate deploy"
    }
  }
}
